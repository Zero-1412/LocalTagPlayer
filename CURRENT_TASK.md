# CURRENT_TASK.md

# 2026-09-05 · 分阶段帧时序、重复启动与升级验收

## 当前

- 从第一性原理出发，后续修改进行对抗式审查，任务结束后自己给出下一步计划。
- 并行研究落地：Agent 实验清单、条件配对及轨迹诊断已实现；34 项工具单测及 validate 通过，独立复审已关闭五项缺陷。十份历史轨迹已核查；真实成对运行及完整目标缺口见 `docs/qa/agent_research_followthrough_20260905.md`。
- 本轮只扩展隔离测量工具与验收记录，生产代码、schema、过滤语义和播放/缓存队列不变。
- 已取得 10 对冷/热独立进程启动、每轮 20 次路由与标签动画、重复扫描的阶段证据。
- 扫描期间查询在 dataRevision 19→20 时连续两轮超时，性能与运行时验收判定失败，未静默修生产行为。

## 最近三项

- 20 次独立进程启动均成功；新/复用 profile 首次搜索 P95 约 1169/1165 ms，超出预算。
- 标签面板动画 build P95 约 24 ms；扫描 UI 构建与事件循环尾部单列，失败等待帧不混入成功分母。
- 全量 712 项通过、4 项既有跳过；新增 recorder focused tests 3 项通过，analyze 0 问题，Windows Debug 构建和 Agent 工具验证通过。

## 阻塞

- 扫描提交期间旧候选失效后当前输入没有发布新结果；两轮均在第 10 次带输入扫描复现，需优先修复。
- 当前进程无法启动 vmms，未获得可丢弃 Windows 用户或 VM；安装升级未验收。
- Computer Use 可读取窗口，但原生输入遇到持续用户输入冲突及目标截图不一致，已停止输入；原生路径未验收。

## 下一步

- 以扫描更新跨越 pending 查询建立 focused 失败测试，确认根因后修复当前输入重新调度，保留 epoch 防污染。
- 按 docs/qa/phase_frames_and_upgrade_acceptance_20260905.md 优先分析扫描、标签动画和结果替换的 UI 成本，再优化首次索引准备。
- 在可丢弃 Windows 环境和无输入冲突时段完成安装升级、目录选择取消、设置与菜单/返回验证。
- 不提前推进搜索历史、可保存筛选、标签便携导出或增量 FTS。
