# 2026-09-05 · 查询可靠性与 Agent 研究落地验收

## 完成合同

以下项目共同组成用户目标。单项通过不代表整体完成；本记录只跟踪当前验收，不替代运行证据。

| 要求 | 当前证据 | 结论 |
| --- | --- | --- |
| 可控 pending 候选跨越扫描提交，旧候选被拒绝，当前输入最终发布 | 并发任务已修改页面回归与零差量接续逻辑，尚待核对最终运行结果 | 未验收 |
| 加载状态最终结束 | 当前已读反例未见显式加载状态断言 | 未完成 |
| 扫描中连续输入、切标签、切排序 | 已见连续输入反例；其余待覆盖 | 未完成 |
| pending 期间返回、切 root、关闭页面 | 待检查页面可达回归，controller dispose 证据不替代页面路径 | 未完成 |
| 新结果进入播放器，来源队列与返回状态保持 | 待本轮运行验证 | 未验收 |
| 旧版本升级保留标签、收藏、进度、stable identity | 既有可丢弃 Windows 环境阻塞尚未关闭 | 未验收 |
| 首次搜索、标签动画、扫描构建成本定位与同场景预算复测 | 并发任务正在修改索引与扫描通知；不能用源码推断性能通过 | 未验收 |
| 固化代码、规则、模型、工具和环境实验身份 | 已接入每个隔离 trial 执行前清单；实际解析模型尚未知，外部工具/资源条件未齐 | 部分完成 |
| 同任务、同预算的旧版/候选版成对比较 | compare 已实现条件检查与配对差量；真实新旧 N=5 运行尚未完成 | 部分完成 |
| 单列错误恢复、误宣告完成、漏验证、重复探索 | 既有评分器有部分结构违规信息，尚未形成目标所需分类汇总 | 未完成 |
| N=5 与统计解释 | 保留既有门槛；文档已明确 5/5 不证明长期零失败 | 文档完成，运行回归按受影响范围执行 |
| 重复文件读取、无关合同/历史读取、截断输出、无变化重复验证 | 已找到忽略的真实轨迹并分析两组 N=5；识别范围与证据限制见下文 | 已有部分分析，不扩大规则 |

## 本轮已完成的确定性验证

- `python -m unittest discover -s test -p agent_eval_tool_test.py -v`：最新 34 项通过。
- `python tool/agent_eval.py validate`：通过。
- 真实临时 Git 仓库回归覆盖相同 tree、规则变更、用例变更、默认配置未知和秘密不导出。
- 当前仓库清单只读 smoke：25 个规则/评测文件；模型解析值 null，未伪造实际模型。
- 日志和 smoke 清单留在 `.local/agent_manifest_*`，不提交原始本地运行产物。

## 边界

本研究任务目前只修改 Agent 评测工具及其测试/文档。并发任务的 Flutter 改动不归入本任务提交。
本任务 schema、FilterQuery / TagQueryService、filtered queue、thumbnail/media queue 均 unchanged；
user data preserved；protected behaviors preserved；unauthorized feature removal none；
mount and reachability 对本次工具增量不适用，完整用户目标中的运行路径仍未完成。
不以扩大提示词、放宽预算或删除失败样本取得通过。

## 轨迹实证与纠正

此前普通 rg 文件清单受忽略规则影响，遗漏实际存在的 trace；已用显式 no-ignore 搜索纠正。
只读分析 `20260719-final-snapshot-level2` 与 `20260721-092857` 的十份规范化轨迹，
结果留在 `.local/agent_trace_audit/summary.json`。两组工具调用分别为 3/0/0/0/3 与 2/3/6/2/0，
与既有 6→13 次记录一致。可识别范围内没有相同参数工具重复、历史目录引用、截断标记或重复验证。
这不证明所有读取必要：变量和复杂脚本读取尚未自动解析，截断也仅识别已知文本标记。

7 月 21 日 trial-3 的六次调用包含宽范围搜索、页面片段、页面搜索、Widget 所在位置搜索、
同一个 Widget 文件的两个不同片段。重复文件引用不等于重复内容，不能据此加入禁止重复读取规则。
非零退出事件中包括接 Select-Object 限量的 rg 输出；仅凭 exit code 不能断言错误恢复失败。
当前证据支持继续按任务局部查询，不支持增加提示词或放宽预算。无文件变化重复验证仍需更强轨迹证据。

## 独立审查修复

首轮审查拒绝交付：实际 schema 未绑定、空模型参数误标显式、CLI 任意错误输出透传。
已针对三项修复，增加实际 schema 与隔离版本不一致、清单先于 Agent 启动、空参数及版本错误输出回归。
后续新增 compare 与 trace-diagnostics 也需最终独立复审。当前全量工具验证曾被并发 ROADMAP
101 行/3534 字符超过 100/3500 门槛拦住；已等义压缩该新增段落，未改优先级或预算；
随后完整 34 项工具单测与 validate 均通过。新增五项 focused 用例也单独通过。
真实历史两组报告执行 compare 得到 incomparable、0 有效配对、5 个试次缺证据，
符合旧报告缺清单不得猜测实验条件的要求，不冒称取得新旧改善证据。

第二轮复审发现实际 judge Rubric 身份遗漏、双方条件同样缺失及非有限指标可能被放行；
已补实际 Rubric 摘要、预算必需项、明确评估状态及有限指标检查，加入针对性回归。
第三轮停止编辑后的独立只读复核通过，允许提交本项工具增量；原五项问题均已关闭。
完整工具单测最终 34 项通过、validate 68 个用例通过；审查还用无写入探针验证边界拒绝。
该结论不覆盖并发 Flutter 改动，不代表完整目标完成；实际新旧成对 N=5、恢复失败分类和运行时缺口仍保留。
