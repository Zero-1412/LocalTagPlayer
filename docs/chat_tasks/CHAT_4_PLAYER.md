## 2026-07-21 文件名与标签入口职责解耦

- 详情文件名铅笔改为“重命名文件”，标签卡片的“添加标签 / 继续添加”继续只打开 manual 标签编辑器；tooltip、ValueKey 和 widget 回归锁定两个独立动作。
- 重命名弹窗只编辑 basename、只读显示原扩展名，并明确提示标签维护位置；输入 controller 随 DialogRoute 真正卸载后释放，快速确认/取消不会触发退场生命周期异常。
- 默认不打断当前播放；文件句柄占用时才复用既有 pause/stop/open/seek 恢复原位置与播放/暂停状态，不修改 `PlayerBackend` contract。
- filtered queue 来源、内容、顺序和当前 index 不变；1249×714 真实窗口只打开并取消弹窗，确认 basename、只读 `.mp4`、禁用确认态及转场无错位、遮挡或溢出，未修改用户媒体文件。

## 2026-07-21 Route 语义、退出画面与反馈收口

- 媒体库使用 `_playerRouteActive` 只在播放器 Route 存续期间排除自身语义，并在 push 前等待一帧提交；播放器根节点声明独立 route scope。该协调不卸载库页、不重建结果列表，也不改变筛选状态。
- `_exitPlayer` 正常路径只等待 pause 确认并发起 pop，最后一帧和当前时间会保留到反向 Route 已开始；`_releaseAsyncResources` 再串行 stop/dispose。pause 超时或失败仍在 pop 前启动 stop，保证音频安全。
- `AppSequentialTransition` 把同容器层级切换分为退出和进入两阶段，播放器列表/详情及播放设置页不再同时绘制新旧文字和控件；reduced motion 继续只保留短淡入。
- 快捷键 HUD 覆盖播放/暂停、前后 seek、上下条、全屏、倍速和音量，并调用原控制条显示链路；队列搜索回调返回 `played / noMatch / emptyQuery`，文案明确提交会直接播放下一条匹配视频。
- 详情路径使用更高对比的连续阅读色，关键字号小幅提升；未收藏项不再常驻空心心形，但保留“未收藏”语义，播放/缺失状态徽标保持紧凑。
- 测试覆盖 Route/退出策略、语义排除、快捷键 live region、搜索三态和顺序转场；完整 229 项测试、静态分析、Windows debug build 与 1249×714 实窗通过。真实返回 70ms 中间帧保留视频纹理和时间，UIA 播放期间不再暴露媒体库节点，返回后恢复。
- filtered queue 来源、内容、顺序和当前 index 未改变；`PlayerBackend`、缓存队列、SQLite schema、标签语义、稳定身份与用户数据均未改变。

## 2026-07-19 删除偏好与可录制返回快捷键

- `PlaybackSettings` 向后兼容持久化删除确认、回收站默认动作和“返回上一页”绑定；旧 JSON 缺字段时保持提示开启、仅移出媒体库与 Esc 返回。
- 媒体库单条/批量删除和播放器队列删除共享 `VideoDeleteDecision`。确认后才保存最终状态；“不再提示”只跳过浮层，不绕过 stable identity 清理或 `FileSystemAdapter` 回收站边界。
- 播放器快捷键从固定候选列表改为真实键盘录制，支持常用基础键和 Control / Alt / Shift 组合；冲突不交换绑定。全屏 Esc 固定安全出口不受自定义返回键影响。
- 非主路由的返回处理器只在当前 Route 生效，弹窗/菜单覆盖时暂停；快捷键录制器拥有焦点时独占下一键，释放焦点后全局返回仍可用。鼠标返回指桌面侧键，不拦截普通左键。
- 221 项测试、静态分析、Windows debug build 和 100%/150% 真机验收通过；filtered queue、当前索引、`PlayerBackend`、SQLite、标签语义及缓存队列未改。

## 2026-07-19 播放设置浮层与控制条视觉收口

- 设置二级页通过局部深色 `canvasColor` 统一继续观看、常用解码和高级解码下拉浮层，文字、当前项与交互反馈恢复 Apple 深色层级；解码值、确认流程和设置持久化不变。
- 普通 `PlayerChromeButton` 使用真正透明的静止基底，消除透明黑被二次赋予不透明度后形成的黑色方块；主播放按钮、键盘焦点和高对比度反馈保留。
- 猫咪主进度焦点调整为真实 28px 矢量绘制，并同步校准两端映射 inset；没有重排控制条、轨道、seek、悬停预览或全屏缩放。
- 完整 214 项测试、静态分析、Windows debug build 与真实窗口两组下拉/播放器截图通过；QA 播放位置恢复到 0:00 暂停状态。
- filtered queue、当前索引、`PlayerBackend`、SQLite schema、标签语义、缓存队列和用户标签/收藏数据均未改变。

## 2026-07-19 猫咪进度焦点与当前文件定位

- 主进度条恢复历史猫耳史莱姆矢量焦点，并继续复用现有 Apple 主题轨道、可访问动效时长和高分辨率全屏缩放；音量滑块不受影响。
- `PlayerRevealFileButton` 文案明确为显示当前视频，页面通过 `PlayerPlaybackController.currentItem.path` 调用既有 `FileSystemAdapter.revealInFileManager`，没有新增或修改平台边界。
- 测试覆盖猫咪焦点视觉契约、按钮 tooltip/回调，以及 `selectedIndex != playingIndex` 时仍定位播放项；真实 Windows 资源管理器确认选中当前播放文件。
- 未修改 `PlayerBackend`、filtered queue 来源/顺序、播放索引、SQLite、标签语义或缩略图/media 队列。

## 2026-07-18 播放器返回复验与三档文字缩放

- 受控 `flutter run -d windows` 连续复现媒体库进入播放器和左上返回，11,163 条来源 filtered queue、媒体库筛选状态、应用窗口与进程全部保持；上一轮进程消失为调试会话/句柄丢失，不修改导航或队列语义。
- Debug 启动链路增加只接受 1.0/1.25/1.5 的文字缩放 QA 开关，Release 不生效；1248×714 三档真实截图覆盖播放器顶栏、视频、队列与控制条。
- 125% 首轮暴露底部时间文本挤压控制条；修复后空间不足只隐藏辅助时间，中央传输控制及文件定位、音量、截图、设置、全屏、队列入口全部保留，125%/150% 复验无溢出。
- 150% 媒体库卡片标题裁切已由媒体库响应式任务修复；人工已完成队列左滑、回拖一半、快速反向并停在展开终态，真实截图确认收藏/删除完整露出且面板等高。
- 完整 196 项测试、静态分析与 Windows debug build 通过，3 项显式 benchmark 跳过；filtered queue、`PlayerBackend`、SQLite schema、标签语义、缓存队列和用户数据均未改变。

## 2026-07-18 Apple UI Phase 2 播放队列操作层与非全屏顶栏联动

- 左滑收藏/删除层只在真实展开进度大于 0 时绘制；完全收起后从绘制与命中树卸载，条目成为当前播放项时另行清除旧进度，避免播放徽标与隐藏动作重叠，同时保留任意条目主动左滑能力。
- 非全屏宽屏队列与 64px 标题栏共用显隐语义：队列折叠后标题栏收起，根表面顶部热区悬停显示、移开隐藏，队列展开时标题栏常驻。全屏队列、当前 filtered queue、播放索引和返回媒体库均不变。
- 根级 hover 只跨越布尔边界时 `setState`；短 `AnimatedSize` 遵守 reduced motion，`accessibleNavigation` 下不隐藏返回入口。focused tests 覆盖收起/展开、播放身份切换、热区、全屏和无障碍门禁。
- 完整 212 项测试、analyze 和 Windows debug build 通过，3 项 benchmark 跳过；真实 1249×714 窗口完成播放项无重叠、队列/顶栏折叠、顶部悬停、移开收回和同步展开截图。
- Phase 3 连续截图补齐媒体库中途隐藏、回顶入口/终态、视频数据备份和标签编辑器；标签编辑器以 Esc 取消。Windows 注入滚轮下 140ms 无闪回，真实触控板惯性仍待人工手感复核。

## 2026-07-18 Apple UI Phase 2 播放器真实窗口验收

- 1248×714 真实窗口连续验证播放/暂停、seek、列表/详情往返；播放图标、时间与进度同步，侧栏切换无残影、遮挡、错位、溢出或视频区域抖动。
- 2560×1440 全屏下，右侧队列以固定覆盖层出现并在离开后自动收起，视频宽度保持；Esc 正常返回窗口模式，当前 filtered queue 序号与筛选摘要保持。
- Windows 自动化三次左滑均只改变队列选中项后改用人工手势；定时自动截图捕获展开终态，无回弹关闭、错位、遮挡或横向溢出，focused widget 与真实窗口证据现已闭环。单键 `F` 仍由 UI 全屏入口代验。
- 初次返回后的进程消失已在受控 `flutter run` 会话中复验为调试会话/窗口句柄丢失；真实返回媒体库、窗口与筛选状态均正常。
- 本轮未修改业务代码、filtered queue、`PlayerBackend`、SQLite schema、标签语义、缓存队列或用户数据。

## 2026-07-18 Apple UI Phase 2 播放器空间与动效精修

- 顶栏使用左右固定动作与真正居中的文件语境；底部 chrome 重排为左侧音量信息、中央传输控制和右侧工具动作，播放/暂停以 46px 主操作和短淡入缩放表达。
- 全屏队列从主 Row 的宽度动画迁到根 Stack 固定覆盖层，短距离滑入/淡入不再逐帧改变视频纹理尺寸；右缘热区、自动隐藏与独立滚动状态保持。
- 列表/详情以 160ms 方向过渡建立空间连续性，旧队列过渡完成即卸载；队列左滑按剩余距离吸附且可被新拖动打断，不引入全列表动画或新 I/O。
- `AppInteractionSurface` 的可选无描边模式用于播放器 chrome，focus/high contrast 仍强制轮廓；reduced motion 取消位移/缩放并保留短淡入。
- focused widget 126 项、完整 194 项、静态分析和 Windows debug build 通过，3 项显式 benchmark 跳过；50,000 条 filtered queue 搜索约 29ms。
- 最新 Debug EXE 已启动并唯一定位；检测到目标窗口正在接收用户输入后停止自动控制，真实点击与截图待按 `CURRENT_TASK.md` 路径补验。
- filtered queue 来源、内容、顺序与当前 index、`PlayerBackend`、SQLite schema、标签语义、缓存队列和用户数据均未改变。

## 2026-07-18 Apple UI Phase 2 播放器全布局

- 播放器视觉层迁入共享 `playerWorkspaceTheme`：顶栏、视频结构表面、浮动控制、右侧列表/详情、设置浮层、恢复面板和删除确认使用同一组 Apple 式深色 token。
- 顶栏保留返回和紧凑队列入口，并增加当前 filtered queue 序号/筛选摘要；队列搜索、二级标签切换、选择/双击播放、收藏、删除、详情、标签编辑、文件定位、截图、诊断、全屏和设置均未删除。
- 控制 chrome 使用小范围实色浮动表面，不引入大面积 blur；侧栏选中态从渐变发光改为中性实色加紫色选择反馈，进度焦点从装饰猫耳收敛为简洁圆点。
- 播放器 30 项 focused tests、完整 193 项测试、`flutter analyze` 和 Windows debug build 通过；50,000 条 filtered queue 搜索基准保持约 25–34ms。
- Windows 自动化启动 Debug EXE 后两次无法激活唯一目标窗口，错误均为 `failed to activate captured window`；真实点击和截图待按 `CURRENT_TASK.md` 的准确路径人工补验。
- filtered queue 来源、内容、顺序与当前 index、`PlayerBackend`、SQLite schema、标签语义、缓存队列和用户数据均未改变。

## 2026-07-17 播放器与卡片统一标签编辑

- 播放器详情不再维护独立的“仅无父级 manual 标签”弹窗数据源，直接复用媒体卡片的当前层级标签编辑入口。
- 同一视频与筛选上下文下，两个入口现在拥有一致的父级标题、folder 锁定项、完整候选集合和 `replaceManualTags` 保存语义；播放器内保存仍延后到返回媒体库再刷新结果，避免播放期间重算标签计数。
- 候选完整性与渲染上限由 Chat 6 公共编辑器修正；完整 164 项测试、静态分析和 Windows debug build 通过。
- 最新窗口自动复测因用户正在目标窗口全屏播放而停止，未抢占键鼠；需人工执行“卡片编辑截图 → 播放同视频 → 详情编辑截图”对比。
- filtered queue、播放/选择索引、`PlayerBackend`、缓存队列、SQLite schema 和用户数据均未改变。

## 2026-07-17 播放器顶部当前文件名

- 顶部栏删除重复的当前队列搜索框，固定应用标题改为当前实际播放视频的完整文件名；队列切换触发页面状态更新时标题同步变化。
- 文件名从跨平台路径提取 basename，单行超长省略并保留完整 tooltip；返回、播放标识与紧凑窗口队列入口不变。
- 清理顶部搜索专用 controller、FocusNode 和 `Ctrl+K` 聚焦分支；右侧列表按需搜索继续使用原 filtered queue 定位逻辑。
- focused widget、完整 162 项测试、静态分析和 Windows debug build 通过；最新 Debug 窗口完成首项播放与双击第二项后的两态截图，无位置、遮挡、对齐或溢出问题。
- filtered queue 来源、内容与顺序、播放/选择索引、`PlayerBackend`、缓存队列、SQLite schema 和用户数据均未改变。

## 2026-07-17 播放器详情底部操作精简

- 右侧详情面板删除底部“编辑标签 / 打开位置 / 更多操作”三项，文件路径卡片成为详情末尾，减少与已有标签编辑及控制条文件定位入口的重复。
- 侧栏组件同步删除只服务上述入口的收藏、文件定位和完整信息回调；标签卡片“继续添加”、文件名右侧编辑图标、队列与控制条行为保持不变。
- focused widget 断言三个入口均不存在，并验证保留的“继续添加”仍进入标签编辑回调；完整 161 项测试、静态分析与 Windows debug build 通过。
- 最新 Debug 窗口完成“进入播放器 → 点击详情”真实验证与截图，文件路径卡片下方留白正常，无位置、遮挡、对齐、溢出或状态反馈问题。
- filtered queue 来源与顺序、播放/选择索引、`PlayerBackend`、缓存队列、SQLite schema 和用户数据均未改变。

## 2026-07-17 播放器静音与键鼠音量控制

- 音量图标升级为一键静音/恢复按钮，页面记录最近一次非零音量；图标、tooltip 和滑条即时同步，不等待异步后端快照刷新。
- 播放器焦点下，上下方向键与视频区域鼠标滚轮按 5 点步长调整 0..100 音量；点击视频画面会恢复播放器快捷键焦点。
- 队列搜索焦点保护上下键和滚轮输入；监听范围限制在视频表面，右侧队列滚轮继续滚动 filtered queue，不改变选择或播放项。
- focused tests 覆盖上下限、滚轮方向、静音恢复、图标和回调；完整 161 项测试、静态分析与 Windows debug build 通过。
- 最新 Debug 窗口完成“点击静音 → 点击恢复 → 连续按方向键 → 视频区域滚轮 → 右侧队列滚轮”真实验证，音量与队列分别按预期变化且无布局异常。
- filtered queue 来源、内容与顺序、播放/选择索引、`PlayerBackend` 契约、缓存队列、SQLite schema 和用户数据均未改变。

## 2026-07-17 播放控制条打开文件位置入口

- 在底部控制条“下一条”和音量控件之间新增弹出式 `eject` 图标按钮，保持控制条现有白色图标、48px 命中范围和 tooltip 反馈。
- 按钮直接复用 `_revealCurrentFile` 与 `FileSystemAdapter.revealInFileManager`，不复制平台命令，也不改变当前播放、暂停、音量或队列状态。
- focused widget 覆盖图标、tooltip 和单次点击回调；完整 159 项测试、静态分析和 Windows debug build 通过。
- 最新 Debug 窗口完成“进入播放器 → 检查按钮位于音量前 → 点击按钮 → 文件资源管理器选中当前视频”真实点击与截图，未见遮挡、溢出、错位或状态反馈异常。
- filtered queue、播放/选择索引、音量链路、缓存队列、SQLite schema 和用户数据均未改变。

## 2026-07-17 队列左滑操作区等高与红心去阴影

- 左滑操作面板显式使用队列条目的完整高度，去掉造成上下边缘不齐的纵向收缩；横向宽度、拖动阈值和 220ms 展开动画保持不变。
- 收藏动作取消半透明粉色底，操作面板取消投影，只保留红色实心/描边心形和原有命中范围。
- focused widget 覆盖卡片与操作面板实际像素等高、收藏表面透明；完整 158 项测试、静态分析和 Windows debug build 通过。
- 最新 Debug 窗口已进入播放器并选中收藏队列项，基础布局无遮挡、错位或溢出；自动化左滑未能让隐藏层保持展开，需人工执行“选中任一队列项 → 向左拖动 → 对比两侧上下边缘与红心背景”补最终截图。
- filtered queue、播放/选择索引、收藏数据写入、删除流程、缓存队列和 SQLite schema 均未改变。

## 2026-07-16 收藏审计、回收站删除与左滑视觉修正

- 只读数据库审计确认收藏写入 SQLite 正常；旧快照仍有 3 条收藏，正式库整批重建视频身份后仅剩 1 条，丢失发生在索引身份重建而不是当前红心按钮写入。
- 单视频确认框把高风险选项改为“移入回收站”；Windows 平台边界显式使用 `SendToRecycleBin`，失败时不继续删数据库记录，且不允许回退到永久删除。
- 左滑动作区由两块大面积暗红背景改为紧凑深色胶囊、细描边、分隔线与低强调图标，继续沿用原吸附展开和移出折叠动画。
- `dart format`、132 项完整测试、静态分析和 Windows debug build 通过，3 项显式烟测/基准跳过；另以环境变量单独启用的真实回收站烟测通过。最新构建窗口确认播放器和收藏状态。自动化拖动未触发展开，随后窗口截图因用户输入和前台进程 ID 缺失中止，左滑展开态与删除弹窗保留人工复测。
- 本轮不自动改写用户正式数据库；SQLite schema、stable identity、filtered queue、标签筛选和缓存队列均未改变。

## 2026-07-15 控制条同步与队列左滑操作

- 底部控制区使用 3 秒统一自动隐藏规则；鼠标驻留进度/按钮区或设置浮层打开时暂停计时，设置关闭后恢复。播放/暂停状态事件始终重建图标，避免控制条已显示时视觉状态滞后。
- 右侧一级切换栏收紧到 34px；队列项保留当前收藏心形，并通过 220ms 左滑动画展开 96px 收藏/删除操作区，移出自动以 180ms 回弹。
- 删除确认明确区分仅移出媒体库与同步删除本地文件；任意队列项均可删除，控制器在删除播放项之前的条目时只回退索引，保持正在播放的视频身份不变。
- 131 项完整测试、静态分析和 Windows debug build 通过。真实窗口已验证侧栏、心形状态和控制条隐藏；设置点击后的截图器无法继续捕获，设置保活、左滑动作及删除弹窗需人工补截图。
- filtered queue 来源、标签筛选语义、`PlayerBackend` 契约、SQLite schema 与缓存队列均未改变。

## 2026-07-15 扫描期间播放让盘

- 媒体库在播放器预检、缩略图预热和解码前暂停正在运行的只读目录扫描；播放器释放后从原候选位置继续，用户手动暂停状态不被覆盖。
- `LibraryScanPlaybackGate` 只协调 Repository 暂停状态，不修改 PlayerBackend、filtered queue、播放索引、EOF 或硬解参数。
- focused tests 覆盖正常退出、用户预先暂停和播放器异常三条恢复路径；媒体探测并发保持不变。
- 隔离真实窗口在 11,163 条强制 fingerprint 扫描中点击首屏视频，约 0.5 秒进入播放器并持续两秒响应；播放期间扫描保持未完成，返回且原生释放后扫描继续。播放器和恢复媒体库截图无遮挡、溢出或错位。

## 2026-07-15 全屏猫耳焦点分辨率自适应

- 普通窗口保持现有焦点尺寸；全屏时按视口短边平滑计算 1.0–1.25 倍视觉倍率，4K 以上封顶，避免高分辨率过小和超宽屏过大。
- 只缩放猫耳焦点的绘制与 preferred size，不改变轨道高度、帧预览、seek、音量条或 filtered queue。
- 2 项 focused tests、静态分析、Windows debug build 与 2560×1440 真实全屏悬停/预览/移出截图通过，未运行无关全量测试。

## 2026-07-15 主进度条猫耳史莱姆焦点

- 使用 Canvas 绘制的紫蓝猫耳史莱姆替换主进度条白色圆点；小尺寸保留明确耳尖、浅色轮廓、高光和简化表情，不引入参考图位图或棋盘背景。
- 猫耳焦点沿用原悬停显隐和按压缩放反馈，仅改变主进度条视觉；音量条圆点、帧预览、seek、filtered queue 与播放索引不变。
- 影响范围测试、静态分析、Windows debug build 和最新构建真实窗口悬停/预览/移出截图通过，未运行无关全量测试。

## 2026-07-15 优酷式主进度条悬停预览

- 主进度条静止时保持 2px 且不绘制圆形焦点；进入后动画加粗到 5px 并显示焦点，离开后恢复。
- 鼠标停稳 350ms 才请求对应时间帧，预览卡显示时间标记；主播放器位置只由用户点击/拖动改变，预览本身不会 seek。
- focused tests、架构 contract、静态分析、Windows debug build 和真实窗口静止/悬停/移出截图通过；filtered queue 与播放索引未改变。

## 2026-07-15 播放器进度与音量条视觉优化

- 主进度与音量滑条统一为圆角渐变轨道和描边滑块，保留原拖动、音量滚轮及播放器状态链路。
- 控制栏自动隐藏后只在视频底边保留 3px 只读进度线；零时长和越界进度会安全钳制，不增加交互层。
- 2 项 focused tests、静态分析和 Windows debug build 通过；真实窗口已验证控制栏显示/隐藏两态，全屏自动化复测切换时应用进程意外退出，需单独复核。
- filtered queue、播放索引、`PlayerBackend`、标签语义和缓存队列均未改变。

## 2026-07-15 精简筛选队列头部

- 队列头部由三层收敛为两层：第一行显示队列名称、当前序号/总数和必要操作，第二行保留搜索。
- 删除固定 AND 说明、重复总数徽标及三个不可操作的默认筛选 Chip；真实二级标签筛选继续保留。
- 队列布局与搜索 2 条 focused tests、`flutter analyze`、Windows debug build 通过，未执行全量测试。
- 实际启动最新 debug 程序进入播放器并截图，头部无遮挡、溢出、错位或对比度问题，列表首屏多显示约一条视频卡片。
- filtered queue、搜索/选中定位、回到播放、缓存队列、SQLite schema 与用户数据均未改变。

## 2026-07-15 移除顶部定位当前播放入口

- 队列标题栏移除与底部“回到播放”重复的准星入口，仅保留“定位已选中”和删除。
- 底部离屏提示继续显示“回到播放 / 回到选中”；保留的播放返回回调统一改名，不改变同步选中态和即时跳转行为。
- 影响范围 focused test、`flutter analyze`、Windows debug build 通过，未执行全量测试。
- 真实窗口确认顶部入口已移除；滚动到第 25–27 项后“回到播放”正常出现，点击后返回第 1 项。
- filtered queue、播放/选择索引、搜索定位、缓存队列、SQLite schema 与用户数据未改变。

## 2026-07-15 列表与详情分段切换蓝图对齐

- 右侧一级视图切换改为连续等宽分段控件，中央无间隙；统一深色底框、外侧圆角和两半区边界。
- 当前视图使用明亮紫色渐变、高亮描边、轻量阴影以及更大的居中图标/文字，切换时只迁移视觉选中态。
- 影响范围 focused test、`flutter analyze`、Windows debug build 通过；未执行无关全量测试。
- 真实窗口点击列表与详情并分别截图，两种状态均未发现遮挡、溢出、对齐或状态反馈问题。
- filtered queue、详情数据来源、播放/选择索引、缓存队列、SQLite schema 与用户数据均未改变。

## 2026-07-15 播放器定位后队列卡片恢复

- 修复大队列滚动或程序化定位后可视项可能永久停留在轻量标题占位的问题；完整卡片恢复不再只依赖 Flutter 的延后加载建议。
- 队列可视层跟踪滚动生命周期，并以最后一个滚动事件后 120 ms 的防抖兼容 Windows 缺失结束通知；滚动期间仍避免启动缩略图校验和媒体详情读取。
- focused 66 项、完整 103 项测试、`flutter analyze`、Windows debug build 通过；真实窗口滚动到第 29–31 项后卡片信息和缩略图完整恢复。
- “定位当前播放”真实点击已触发；随后截图受既有 Windows Flutter 无障碍桥异常阻断，需在不启用自动化辅助桥的桌面环境补一次点击截图。
- filtered queue、选择/播放索引、`PlayerBackend`、缓存队列、SQLite schema 与用户数据未改变。

## 2026-07-15 播放器列表与详情同级侧栏

- 播放器底部不再保留当前视频信息卡，视频画面直接使用释放出的空间；文件名、标签、媒体参数、路径和相关操作迁入右侧详情视图。
- 右侧“列表 / 详情”保持同级切换；列表继续使用来源 filtered queue 与当前索引，详情始终读取实际正在播放的 `VideoItem`，不改变单击选择、双击播放或定位行为。
- 详情切换只消费现有缓存字段，不触发新的文件系统读取、媒体探测或缩略图任务；隐藏视图不挂载，避免不可见列表产生额外构建与 I/O。
- widget focused 65 项、完整 102 项测试、`flutter analyze`、Windows debug build 和真实窗口点击/截图验证通过；未修改 schema、`FilterQuery` / `TagQueryService`、`PlayerBackend` 或缓存队列。

## 2026-07-14 队列选择、双击播放与稳定定位

- 播放器仍消费媒体库当前 filtered queue 的会话快照，不访问全媒体库或重新筛选；列表容器隔离会话修改，但继续复用原 `VideoItem` 的稳定身份和缓存字段。
- 队列项恢复为单击只更新选中态、双击才切换播放；单击不自动滚动，避免双击第二击因列表移动而落到其它视频。
- “定位当前播放”会先把选中态同步到真实播放索引，“定位已选中”只滚动不切换播放；首次布局未建立时有限重试，大队列显式定位使用居中即时跳转。
- 真实窗口确认 `1/173` 下单击第 2 条仍播放第 1 条，双击后准确进入 `2/173`；自动化整体定位受 Windows Flutter 无障碍树原生异常阻塞，偏移、选择和播放状态由 focused tests 覆盖。
- filtered queue 来源、连续播放边界、`PlayerBackend`、SQLite schema 和用户数据语义未改变。

## 2026-07-14 PlayerBackend 释放后 60 秒长尾

- 压测等待最后一个真实 `released` 后继续驱动 Flutter 帧，并在 0/5/15/30/60 秒记录 RSS、ImageCache、线程、句柄和有效 GPU counter；不在 PlayerPage 调用 GC 或清理 ImageCache。
- 一轮有效 GPU 对照中 Private 643.5→591.7 MiB、GPU committed 144.7→104.6 MiB、线程 128→125；主要回落发生在前 15–20 秒，证明 libmpv/D3D 销毁后仍存在异步驱动释放长尾。
- 60 秒末 GPU committed 只比播放前新增库滚动阶段高约 4.5 MiB，线程更低；Private 仍高约 52 MiB，后续应在 PlayerBackend、libmpv allocator 与驱动分配池边界继续区分稳定复用池和跨轮泄漏。
- filtered queue、播放索引、硬解选择和退出状态机未修改。

## 2026-07-14 4K 硬解矩阵与超规格预检

- RTX 4070 SUPER / 驱动 595.97 / MediaKit `d3d11va-copy` 下，真实 4K/60 H.264、HEVC、AV1 各两轮均启用实际硬解，视频/音频停滞为 0，AV offset 最大约 0.000445 秒。
- `PlayerHardwareCompatibility` 只读取已缓存 `MediaDetails`；标准 4K 三种编码避免误报，未知规格保持 unknown，不在播放入口启动 FFprobe。
- 已确认回退软件解码的 8K/60 H.264 在首次路由和 filtered queue 切换的新 open 前提示；队列取消恢复已打开项，确认后才提交路径，并给出不覆盖源文件的 4K H.264 代理与 4K HEVC 转码建议。
- filtered queue、播放索引、PlayerBackend 打开参数与退出状态机未修改。
- 87 项测试、analyze、Windows debug build 通过；真实窗口截图验证取消/继续/返回链路和弹窗布局。

## 2026-07-13 实际硬解持续复核

- 每秒健康采样增加 `hwdec-current`，属性不可用不再直接等同于软件解码；已开始播放时的空值按 mpv 软件解码语义处理。
- `hwdec-codecs=all` 只在 open 前设置；连续三次明确为软件解码后只记录真实回退，不在当前会话热切换后端，避免超规格 H.264 从可播放退化为 `media_kit_error`。
- RTX 4070 SUPER 真实 7680×4320@60 H.264 样本采到 `hwdec-current=no`、总掉帧 4,240、AV offset 约 2.4 秒；这是本轮明确保留的软件兼容回退，4K 常规样本仍沿用既有 `d3d11va-copy` 验证基线。
- filtered queue、播放索引、退出状态机和 PlayerBackend 接口均未修改。

## 2026-07-13 Player/纹理/GPU内存阶段归因

- Player构造、纹理就绪、媒体打开、pause、stop、dispose与返回媒体库延迟阶段输出可对齐的内存标记。
- 五轮实测中mpv demux在stop后清空，D3D Shared在Player dispose后约2秒回落，纹理ID同步失效。
- 返回媒体库后NVIDIA Dedicated/Committed仍保留数百MiB但会回落，线程与句柄不累积；当前按驱动缓存/复用池处理，不采用全局Player或强制工作集清理。
- filtered queue、播放索引、标签语义和SQLite schema均未修改。

## 2026-07-13 压测录屏逐帧分析

- 录屏与压力计时通过 marker 握手，确保三分钟样本不包含大型媒体库初始化阶段。
- 30 fps 录屏逐帧差分发现约 12 秒阶梯更新；对应测试把 `pumpAndSettle` 的位置参数误作超时，实际将单次 pump 步长设置为数秒。
- 长等待统一改为每 50 ms pump，媒体库等待改为真实播放按钮就绪；修正验证中进入播放器到队列滚动稳定为约 12 秒。
- 本轮未关闭语义树、未使用 Windows UIA，也未修改 filtered queue、PlayerBackend 或播放器业务行为。

## 2026-07-13 D3D11VA 直连与长驻实例评估

- 推荐硬解从 `auto-copy` 收敛为 `d3d11va-copy`，真实媒体峰值线程由 283 小幅降到 279，Private 峰值约下降 30 MiB。
- 两轮退出的原生 dispose 均在约 2–20 ms 内完成，下一轮没有线程或句柄累积。
- 全局长驻 Player 不会降低播放期间的单实例峰值，只会让 D3D11/NVIDIA 驱动线程在媒体库继续驻留，因此不采用。
- PlayerBackend 后续只负责串行会话所有权与释放；单实例线程优化继续留在 media_kit/libmpv 视频输出平台边界。

## 2026-07-13 播放器原生资源与推进检测

- 单播放器进入后线程峰值仍约 281–283；固定 lavc 线程后峰值不变，确认主要增量位于 media_kit/libmpv 的 D3D11/NVIDIA 视频输出边界，而不是 filtered queue 或 FFprobe。
- 输入和 demux 缓存组合预算收敛后，真实媒体工作集峰值约下降 227 MiB；继续保留 `d3d11va-copy` 可靠硬解。
- 每秒分别采样 `estimated-frame-number` 与 `audio-pts`，可独立报告视频冻结、音频停顿及两路同时停顿。
- 返回媒体库前确认 pause，随后记录 pop 与原生 dispose 完成时间；两轮随机循环均完成释放。

## 2026-07-12 accessibility bridge 隔离验证与无 UIA 截图

- 新增 Windows 桌面集成测试，通过 Flutter Finder/VM Service 点击队列按钮，并以桌面像素采集补拍折叠、恢复状态，不查询 Windows UI Automation 语义树。
- 队列浮动定位器在 `ScrollPosition` 已绑定但内容尺寸尚未建立时按可见处理，避免首次挂载与恢复动画的 viewport 空值竞态。
- stable 3.44.4 与隔离 beta 3.46.0-0.3.pre 均通过无 UIA 用例；由于问题 UIA 客户端不能枚举 beta 测试窗口，不能据此宣称引擎 bridge 已修复，生产 SDK 保持 stable。

## 2026-07-12 长视频隔离复测与辅助功能桥排查

- 将 `purple-grid.mp4` 同路径替换为 600 秒循环样本，现有隔离数据库无需改路径即可复测。
- 截图确认菜单 `bottomEnd` 方向错误；改为 `topEnd` 后菜单从齿轮下方向下展开。
- 长视频排除了 EOF 假设；退出前日志只有 Flutter Windows AXTree 与 `ax_platform_node_win.cc` 错误，没有播放器业务或媒体内核异常。
- 不以 `ExcludeSemantics` 关闭播放器可访问性作为规避；队列折叠/恢复仍需在不启用问题辅助功能桥的真实桌面环境补拍。

## 2026-07-12 播放器菜单方向与队列入口收敛

- 齿轮菜单通过 root overlay 与底部对齐固定向下展开，不再优先向上遮挡画面。
- 全屏后的按钮改为播放队列图标；宽屏折叠后不在顶栏生成第二个队列入口。
- 窄屏仍通过顶栏入口打开底部队列；filtered queue、滚动保活和播放语义未改变。
- 自动启动与点击已执行；隔离短视频在浮层触发后进程退出，截图接口失去目标，需以稳定长视频补拍菜单方向截图。

## 2026-07-12 播放器二级菜单与侧栏保活

- 当前帧截图恢复为控制栏独立按钮；播放速度和播放模式迁入齿轮的二级菜单。
- “更多”菜单从按钮下方展开；宽屏队列折叠使用零宽保活，不重新创建 filtered queue 列表。
- 全屏 Escape 固定优先退出，并在设置页快捷键卡中明确展示；非全屏原有 Escape 返回逻辑保持不变。
- 未修改 filtered queue 来源、播放模式语义、诊断采样、标签数据或 SQLite schema。

## 2026-07-12 播放器控制去重与桌面全屏

- 倍速、播放模式、快捷键、截图和诊断收纳到控制栏齿轮菜单；信息卡“更多”和队列头部删除重复入口。
- 全屏改用桌面窗口全屏，进入后只保留视频与控制层；全屏按钮后新增筛选结果队列折叠按钮。
- filtered queue、当前 index、EOF 行为、标签语义和诊断采样逻辑均未改变。
- 隔离真实窗口确认精简控制栏、队列去重和真正全屏；71 项测试、analyze 与 Windows debug build 通过。

## 2026-07-12 播放器弹窗内部卡片统一

- 标签编辑器按编辑范围与标签选择分组，搜索、新建、锁定来源、最近使用、收藏和键盘链路保持不变。
- 视频信息按文件、媒体、整理状态和异常分组；播放诊断按实时状态、分析结论和详细指标分组。
- 共享卡片组件只负责视觉排版，未修改 `PlayerBackend`、filtered queue、标签数据语义或诊断采样逻辑。
- 隔离真实窗口逐一完成模拟点击与截图复核；71 项测试、analyze 和 Windows debug build 通过。

## 2026-07-12 播放器控制层重排

- 自定义 media_kit controls builder 只负责 UI：底部统一进度、播放/暂停、前后项、时间、倍速、音量与全屏，播放中空闲三秒淡化。
- 下方信息区移除大按钮和路径常驻展示，标题单行 tooltip、队列序号醒目，标签紧凑保留，文件位置等低频动作进入更多菜单。
- 右侧队列顶部缩短为筛选摘要与当前序号，不重复展示多套 chips/计数。
- 真实窗口确认 1280×720 下控制条和信息区无溢出；未改变播放器 open、EOF、filtered queue 或快照写入。

## 2026-07-12 继续观看默认行为

- `PlaybackSettings` 新增继续观看默认策略，旧设置缺少字段时默认从上次位置继续，不再逐条弹窗打断。
- 设置页可切换“从上次位置继续 / 从头播放 / 每次询问”；只有每次询问继续复用原恢复弹窗。
- 播放位置、完成阈值、videoId 绑定和快照写入保持不变。

## 2026-07-12 播放器上下文与对比度 P1 修复

- 右侧 filtered queue 使用媒体库生成的同源摘要并显示当前项数；真实窗口确认“原神 / 雷神 + 关键词 raiden”和 41 项一致。
- 底部上一条、编辑标签和打开位置使用高对比度边框样式；下一条使用主色填充；播放模式与倍速菜单改为暗底亮字。
- 未修改队列来源、EOF 策略、schema、标签过滤语义或缓存队列。

## 2026-07-12 第四阶段隔离真实窗口 smoke

- 隔离 profile 扫描两条真实 18 秒 H264/AAC 媒体，点击确认四种播放模式菜单、随机 EOF 队列切换、0.5x 至 2.0x 六档倍速和 1.5x 当前状态。
- 全屏可见 `2 / 2` 队列上下文和“编辑标签”，folder 来源标签保持只读。
- 首轮发现标签弹窗 Escape 冒泡会误退回媒体库；增加标签编辑期间的页面级 Escape 拦截后，复测只关闭弹窗/全屏并继续停留于播放器。
- 未修改 schema、filtered queue 来源、标签过滤语义、缩略图/media queue，也未进入字幕或音轨建设。

## 2026-07-08 播放页面队列 UI 拆分

- `player_page.dart` 保留播放器生命周期、跳转、快捷键、播放诊断采样和页面级状态协调。
- 底部当前视频与筛选上下文摘要迁到 `pages/player_context_panel.dart`。
- 右侧筛选结果队列、队列项、子标签切换、定位按钮和 `playerQueueIndexIsVisible` 测试 helper 迁到 `pages/player_queue_sidebar.dart`。
- 本轮只做 part 文件拆分，不修改 filtered queue 来源、当前 index、右侧二级标签切换语义、`PlayerBackend` 或缩略图/media 队列。

# CHAT_4_PLAYER.md

## 2026-07-15 全屏设置锚定、动画与控制条保活

- 设置浮层改为锚定齿轮按钮的全局矩形，全屏与普通布局均在按钮上方右对齐；矮屏通过高度约束与内部滚动避免越界。
- 打开浮层使用缩放淡入，一级/二级使用左右滑动淡入；移除右上关闭按钮，保留空白区域与 Escape 关闭路径。
- 设置展开期间控制条自动隐藏条件被显式阻断，关闭后重新启动既有 3 秒计时；不改变播放器播放状态或队列。
- 设置/控制条 3 项 focused tests、静态分析、Windows debug build 和 2560×1440 全屏真实点击截图通过；未运行无关全量测试。
- filtered queue、播放索引、PlayerBackend、标签/SQLite、缩略图与媒体详情队列均未改变。

## 2026-07-15 播放器一级/二级设置与镜像

- 播放设置一级页收敛为镜像、单曲循环、列表循环和“更多播放设置”；比例、倍速、完整播放方式、快捷键与诊断迁入同一浮层的二级页。
- 循环开关互斥并复用既有 `PlayerPlaybackMode`；镜像通过 `PlayerBackend` 可选参数仅翻转视频表面，控制条与 filtered queue 不参与变换。
- 聚焦测试、架构契约、静态分析、Windows debug build 和真实窗口四条点击路径通过；未执行无关全量测试。
- SQLite schema、标签查询、filtered queue 来源、缩略图/媒体详情队列和用户数据均未改变。

## 2026-07-12 全屏队列同级挤压布局与局部重置

- 全屏队列迁入播放器主 Row，显示时占用固定 440px 右栏并让视频列缩窄，隐藏后视频恢复全宽，不再覆盖画面。
- 普通窗口和全屏布局使用独立滚动控制器，定位按钮作用于当前可见列表；队列数据、当前索引和播放跳转仍完全共享。
- 设置页“恢复默认”只调用 `resetFullscreenQueueInteraction` 恢复 12px / 180ms，不改变硬解、继续观看或快捷键。
- 集成测试断言展开时视频宽度减少超过 400px、隐藏后恢复原值、边缘唤出再次缩窄，并完成真实窗口截图。

## 2026-07-12 全屏队列边缘参数设置

- `PlaybackSettings` 新增全屏队列热区宽度与自动隐藏延迟，默认 12px / 180ms，JSON 读取兼容旧设置并限制为 4–40px、0–1000ms。
- 设置页新增独立卡片和两个滑杆；拖动时本地预览，`onChangeEnd` 才持久化，避免连续磁盘写入。
- 播放器边缘热区和隐藏 Timer 直接消费持久化设置，不改变 filtered queue、当前索引或列表滚动行为。
- focused test 覆盖默认值、旧 JSON、边界约束、复制和序列化；隔离 profile 实际修改后重新进入播放器回归通过。

## 2026-07-12 全屏队列右侧边缘抽屉

- 全屏播放列表由模态 Overlay 改为播放器根 Stack 内的右侧抽屉，不显示遮罩，也不阻断视频区域交互。
- 播放列表按钮和屏幕右侧 12px 鼠标热区共用展开状态；鼠标离开 440px 队列宽度后延迟 180ms 自动收回。
- 隐藏状态不挂载队列 ListView，避免全屏切换帧与普通侧栏短暂复用同一个 `ScrollController`。
- 长视频集成测试覆盖按钮展开、离开自动隐藏、右边缘再次唤出及两张真实窗口截图。

## 2026-07-12 播放首帧预热与全屏队列闭环

- 进入播放器前预热当前视频附近最多九项已生成缩略图，队列 `FutureBuilder` 使用同步内存缓存作为首帧数据，避免路由后占位底色闪烁。
- 初次播放不再同步启动相邻队列媒体探测；当前媒体确认可播放后延迟 600ms 再预取，降低大文件首次解码与 FFprobe、磁盘读取竞争。
- 音量图标和滑杆区域支持滚轮调节；控制层空闲后完全透明；全屏播放列表通过根 Overlay 从右侧滑入并继续消费同一 filtered queue。
- Windows 长视频集成测试验证列表预热、折叠/恢复、滚轮音量、控制层隐藏及全屏列表，截图器按 DPI-aware 窗口物理矩形采集。

## 2026-07-12 窗口恢复、快捷键配置与统一菜单

- `DesktopWindowStateService` 通过 `window_manager` 恢复窗口尺寸和最大化状态，延迟合并 resize 写入独立 JSON。
- 播放器页面移除快捷键提示；设置页可修改十项快捷功能，按键冲突时交换绑定，旧设置自动补默认值。
- 播放器范围启用统一暗色 Dialog/PopupMenu/Input 主题，“更多”菜单高对比显示；全局弹窗、菜单和底部面板统一基础样式。
- 隔离真实窗口截图覆盖设置页、快捷键冲突交换、播放器无提示栏和更多菜单；未修改 filtered queue、EOF、PlayerBackend 或 SQLite schema。

## 2026-07-12 播放器信息卡与快捷栏蓝图对齐

- 信息卡上层展示带扩展名标题、编辑入口、路径，以及时长、大小、封装/编码和分辨率摘要；下层展示标签 chips、添加标签和主要操作。
- 收藏移入“更多”，避免占用蓝图主操作区；快捷键栏改为 Space、J/L、T、F、S，新增动作均复用现有标签、全屏和截图链路。
- focused tests、analyze、Windows debug build 和隔离真实窗口布局复测通过；未修改标签来源、filtered queue、EOF、PlayerBackend 或缓存队列。

## 2026-07-12 播放器红框控制层精确重排

- 以用户 1920 宽红框截图为基准，重新分配左侧前后项、播放、音量和时间位置，控制时间固定显示两位小时。
- 右侧增加可用的快捷键提示、media_kit 当前帧截图和现有播放模式菜单，与倍速、诊断和全屏形成蓝图式工具组。
- 1100px 以下窗口不再常驻右侧队列，避免完整控制组溢出；未修改 filtered queue、EOF、PlayerBackend 或缓存队列。

## 2026-07-12 播放器蓝图截图对比与控制区深度对齐

- 创建隔离 profile 和三条 1280×720 H264/AAC 视频，真实进入播放器截图，并对比蓝图的视频、控制、信息、快捷栏和队列五个区域。
- 进度与音量使用蓝紫细轨，控制顺序改为上一条、播放/暂停、下一条、音量、时间、倍速、诊断、全屏；未增加未实现的字幕或画中画入口。
- 队列增加只读筛选状态 chips、暗色搜索、104px 卡片和 100×58 缩略图，播放态改为蓝紫边框；快捷栏全宽，信息卡减少垂直占用。
- focused tests、analyze、Windows debug build 与隔离真实窗口复测通过；未修改 filtered queue、EOF、PlayerBackend、标签语义或缓存队列。

## 2026-07-12 播放器蓝图双栏比例修正

- 右侧 filtered queue 宽度改为桌面窗口的约 30%，并限制在 360–500px，保持宽屏信息密度与蓝图一致。
- 队列容器顶边与左侧视频画面统一为 18px；focused test 覆盖 960、1280、1600 和 1920 宽度。
- 未修改 filtered queue 来源、当前 index、EOF、PlayerBackend、标签语义或缩略图/media queue。

## 2026-07-11 播放快照串行写入

- `PlaybackSnapshotWriteQueue` 以稳定 videoId 为合并键，同一视频只保留最新待写状态。
- 全部 SQLite upsert 严格串行，播放器返回媒体库前 flush；失败不会阻塞其它视频并显示稳定提示。
- focused test 验证 `1s→2s→3s` 只写入 `1s、3s`，最大并发 writer 为 1。

## 2026-07-11 继续观看与完成态

- 打开有效进度视频时暂停并询问“从上次位置继续 / 从头播放”，选择期间禁止 0 秒事件覆盖旧进度。
- 低频写入、切换、退出和 EOF 统一保存位置、总时长和完成态；最近播放升级为带进度条的继续观看。
- 短视频尾部阈值为 1-2 秒；长视频按 5% 且限制在 5-30 秒，接近结尾不恢复。
- 队列 missing 项显示缺失状态并停止失效路径预取；失败面板支持 Relink 和跳过，不删除稳定记录。

## 2026-07-11 键盘导航与大队列基准

- manual 标签弹窗支持自动聚焦、Tab/Shift+Tab 候选遍历、Enter 添加、Ctrl+Enter 保存和 Escape 取消。
- 新增 50,000 条 filtered queue 最坏命中基准；本机约 24ms，使用 2 秒宽松上限防止全库访问或超线性退化。
- 基准函数只消费传入队列，不访问媒体库 store、SQLite、缩略图或文件系统。

## 2026-07-11 标签播放器差异化第二阶段

- 播放页提供编辑标签、收藏和打开文件位置，用户无需返回标签中心即可整理当前视频。
- 右侧队列搜索只遍历当前 filtered queue 并直接定位播放，来源队列、当前序号和返回媒体库筛选状态保持不变。
- 收藏和打标只更新当前视频与必要索引；播放器会话中不刷新全库标签计数，返回后只做无计数轻刷新。
- 文件管理器定位通过 platform 边界执行，播放器 UI 不包含 Windows 命令。

## 2026-07-11 稳定身份播放进度

- 播放位置不再按 path 单独记忆，而是保存在稳定 `videoId` 对应的视频记录中；文件移动并自动 relink 后继续沿用原进度。
- 播放中约每 5 秒低频写入，切换或退出时补写；open 成功并确认可播放后恢复位置。
- 距离结尾不足 5 秒的进度不恢复，EOF 后清零，避免自动续播闭环被旧进度重复触发。
- 本轮未改变 filtered queue 来源、连续播放顺序、`PlayerBackend` 或 `FilterQuery` / `TagQueryService` 语义。

## 2026-07-10 隔离坏文件 smoke 与播放器标签入口

- `Player.open` 返回后会检查时长和音视频编码是否至少有一项可用；真实 0-byte MP4 不再停留在假播放状态，而是进入稳定 `unplayable_media` 恢复面板。
- 隔离 profile 的三条队列 smoke 覆盖坏文件诊断与跳过：单条损坏不会阻塞后续正常 H264/AAC 文件。
- 播放器上下文面板新增“编辑手动标签”，保存后刷新当前视频信息但不重建或替换来源 filtered queue。
- 播放进度记忆明确等待 Stable Video Identity 后实施，本轮不新增 path 绑定的进度持久化。
- 本轮未修改 schema、`PlayerBackend`、filtered queue 来源、`FilterQuery` / `TagQueryService` 或缩略图/media 队列。

## 2026-07-10 连续播放与错误恢复闭环

- `PlayerPage` 订阅 `media_kit` 完成事件，只在库页传入的 filtered queue 内顺序推进；队尾停止并持续显示完成提示，不默认循环。
- 播放器上下文面板增加显式上一条/下一条按钮；原有 `PageUp / PageDown` 快捷键保持不变。
- `PlayerOpenRequestController` 增加可恢复失败状态；稳定错误面板提供重试、跳过和诊断详情，同时避免把异常正文和本地路径写入可复制摘要。
- 播放诊断入口移动到顶部，诊断弹窗支持复制不含本地路径的摘要，并显示弹窗内复制完成态。
- focused tests 覆盖顺序队列边界、队尾不循环、快速 open 最新请求和失败重试；真实窗口覆盖 EOF 自动下一条、队尾停止、诊断复制和返回筛选状态。
- 本轮未修改 `PlayerBackend`、filtered queue 来源、SQLite schema、`FilterQuery` / `TagQueryService` 或缩略图/media 队列。

当前版本：`0.3.0`
状态：进行中
负责人：Chat 4 / 播放器筛选队列 + PlayerBackend

## 规划来源

主要来源：

```text
<private-planning-document>
```

如果本文档与该文件冲突，以外部规划为准。

## 范围

负责播放稳定性、筛选播放队列消费、硬解、右侧队列行为、诊断、`PlaybackSession` 和 `PlayerBackend` 实现。

允许：

- `PlayerPage` 和未来 `PlayerService`。
- 右侧队列 / 列表行为。
- 播放诊断。
- `PlaybackSession` 消费。
- 未来 `platform/player`。

禁止：

- 标签数据库 / schema 重设计。
- 缩略图队列内部逻辑。
- 大范围 UI 视觉 polish。
- 当前阶段未明确要求时，不做字幕、音轨、逐帧、A-B loop、滤镜、复杂比例控制等高级播放器功能。

## P0 / P1 任务

- 播放器队列必须代表当前筛选结果，而不是无关全局列表。
- 进入和返回播放器时保留筛选上下文。
- 增加或维护当前序号显示，例如 `1/1661`。
- 右侧队列标题应概括当前筛选。
- 支持从右侧队列切换视频。
- 保持视频信息入口稳定。
- 保持播放诊断入口稳定。
- 后续让诊断信息可复制。
- 在不重写播放器核心的前提下，把播放页逐步移到 `PlayerBackend` 后面。
- 添加高级播放器功能前，先修复 async open / jump 竞态风险。

## 新对话提示

```text
这是 Chat 4 / 播放器筛选队列 + PlayerBackend。项目路径：<project-root>。
请先阅读：
- PROJECT.md
- ARCHITECTURE.md
- CURRENT_TASK.md
- ROADMAP.md
- <private-planning-document>
- docs/chat_tasks/CHAT_4_PLAYER.md

职责：播放器消费当前筛选队列、维护右侧队列、播放诊断、PlaybackSession 和 PlayerBackend。
不要修改标签 schema、缩略图队列或无关视觉重构。标签发现闭环稳定前，不优先做专业播放器增强。
修改代码后运行：
- flutter analyze
- flutter build windows --debug
```

## 变更记录

- `0.4.1`：按播放器产品蓝图对齐页面级布局。新增品牌顶栏、当前队列搜索、丰富视频身份卡、队列总数徽标和快捷键提示；排除蓝图右上角“打开文件”，继续由媒体库筛选结果进入播放器。完整测试、analyze 和 Windows debug build 通过；默认大型媒体库启动持续停留加载层，隔离媒体点击复测待补。
- `0.4.0`：完成第四阶段轻量播放增强。新增顺序、随机、单曲循环、列表循环和六档倍速；补充空格、J/L、`[`/`]` 高频快捷键；全屏控制层展示当前 filtered queue 序号、筛选标题和编辑标签入口。未修改 schema、队列来源、标签过滤语义或缓存队列，也未提前建设字幕和音轨能力。

- `0.3.0`：完成第一轮播放器筛选队列：`PlayerPage` 消费安全的筛选队列副本，展示筛选摘要上下文，右侧二级标签切换保持在筛选队列内，并串行化快速 open 请求，让最新 jump 生效。验收修复统一了空筛选标题、序号显示和待处理 open 排空行为。
- `0.2.0`：按外部跨平台规划重定播放器任务，聚焦筛选队列消费和 `PlayerBackend`。
- `0.1.0`：创建任务模板。

## 2026-07-08 播放诊断与播放状态协调拆分

- 新增 `PlayerPlaybackController`，集中维护来源播放队列、当前二级标签、正在播放索引和选中索引；页面仍负责 mpv 打开、快捷键、删除确认和 UI 生命周期。
- 新增 `player_diagnostics_dialog.dart`，承接播放诊断弹窗、连续采样 timer 和播放状态订阅；关闭弹窗时继续释放异步资源。
- 本轮未修改 `PlayerBackend`、filtered queue 来源、当前 index 展示、右侧二级标签切换语义或缩略图/media 队列。

## 2026-07-08 播放 open 请求与删除确认拆分

- 新增 `PlayerOpenRequestController`，集中最新待打开路径、open worker 运行状态和打开中遮罩状态；`PlayerPage` 继续负责实际 `Player.open` 调用和 mpv 参数设置。
- 新增 `player_delete_dialog.dart`，承接删除视频文件确认弹窗，后续可继续扩展删除影响提示。
- 本轮未修改 `PlayerBackend`、filtered queue 来源、当前 index 展示、右侧二级标签切换语义或缩略图/media 队列。

## 2026-07-08 播放状态协调测试补强

- 新增 `PlayerPlaybackController` focused test，覆盖二级标签切换、再次点击取消二级筛选，以及二级标签无结果时回退来源过滤队列。
- 新增 `PlayerOpenRequestController` focused test，覆盖 open worker 运行中失败或结束前仍保留最新待打开请求，避免快速切换时丢失最后一次打开意图。
- 本轮未修改 `PlayerBackend`、filtered queue 来源、当前 index 展示、右侧二级标签切换语义或缩略图/media 队列。
# 2026-07-13 播放器滚动与 4K 长播稳定性

- 队列快速滚动期间不创建会触发磁盘校验和媒体探测的完整条目，减速后再恢复缩略图与详情展示。
- 将队列预建范围从约七项收窄到约两项，并把播放后的媒体详情预取限制为当前视频。
- 播放器输入缓冲提升到 128 MiB；mpv 使用受限 30 秒预读与缓存耗尽暂停，降低高码率长视频持续丢帧追赶的风险。
- 未修改 filtered queue 来源、当前索引、播放模式或标签筛选语义。
# 2026-07-13 PlayerBackend 完整会话与纹理边界

- `PlayerBackend` 现统一暴露播放命令、轻量状态、位置/播放/完成/错误流、纹理 ID、诊断属性、截图、视频表面和原生释放完成信号。
- `MediaKitPlayerBackend` 独占现有 Player 与 VideoController；`PlayerPage`、诊断弹窗和内存阶段采样只通过后端契约访问，不再穿透 libmpv 平台对象。
- 页面构造支持 `PlayerBackendFactory` 注入，默认继续使用现有 media_kit/libmpv 与 `d3d11va-copy`，后续 C++ 后端可做可回滚 A/B 切换。
- filtered queue 来源、当前 index、标签语义、缩略图/媒体详情队列和 SQLite schema 均未修改。
- 74 项测试、analyze、Windows debug build 与 90 秒真实媒体库 4 轮回归通过；视频/音频停滞事件均为 0，退出后纹理 ID 均失效。
# 2026-07-13 Windows C++假纹理与生命周期骨架

- runner注册`local_tag_player/native_player`通道，原生层拥有外部纹理、生命周期状态和串行命令线程。
- Flutter增加`WindowsNativePlayerBackend`，只允许环境开关`windows-native-stub`显式启用；生产默认继续使用media_kit。
- 假后端真实媒体库2轮回归确认棋盘格纹理、队列滚动、全屏、seek和退出链路；每轮dispose后纹理ID变为`-1`。
- 默认media_kit同媒体2轮对照仍为`d3d11va-copy`，seek约27ms，视频帧与音频PTS持续推进且无停滞事件。
- 假后端峰值173线程/565.6MiB工作集/558.9MiB GPU committed，对照为315线程/830.3MiB/781.8MiB；由于假后端不解码，这组数据仅建立桥接开销基线，不宣称性能提升。
- 真正libmpv/D3D11尚未接入：当前依赖只存在于生成目录，必须先建立固定版本第三方依赖供应，不能提交机器相关链接路径。

# 2026-07-13 Windows 原生 libmpv/ANGLE 实链

- 构建按 SHA-256 固定供应 libmpv、ANGLE 和 Windows 纹理桥接 C++ 源码，运行库及许可证随 Windows bundle 安装。
- 原生单工作线程串行拥有 `mpv_handle`、`mpv_render_context`、ANGLE 表面与 D3D11 共享纹理；mpv 更新回调不直接渲染，避免回调重入死锁。
- EOF、错误、帧推进、音频 PTS、AV offset、缓存、掉帧、帧率和实际硬解进入 `PlayerBackend` 节流快照；filtered queue、当前 index、SQLite 与缩略图/media 队列不变。
- 同媒体 20 秒 A/B 中两端均使用 `d3d11va-copy` 且无音视频停滞；原生 seek 更低但释放更慢、GPU committed 峰值更高，因此默认仍保持 MediaKit，原生后端只由 `windows-native-mpv` 显式启用。

# 2026-07-13 4K 长视频分阶段 A/B

- 压测可固定同一真实 4K 长视频，并把进程指标按启动、稳定播放、释放和媒体库空闲分组；汇总脚本输出中位数、P95 和峰值，不再用启动峰值代表稳定播放。
- MediaKit、原生基线和原生优化各完成 480 秒/18 轮同种子操作，均为 `d3d11va-copy`，音视频停滞和进程无响应均为 0。
- 原生后端按 `MPV_RENDER_UPDATE_FRAME` 决定是否渲染，ANGLE 表面跟随 Flutter 请求量化，纹理读取与渲染分别计数；demux 预算收敛到 64+16 MiB。
- 优化消除了 seek 高尾并降低工作集、Private 和 GPU P95，但原生 Private/GPU committed 仍高于 MediaKit；filtered queue、当前 index、SQLite、标签语义和缩略图/media 队列均未修改，默认后端不变。

# 2026-07-13 D3D11/ANGLE 最终门槛判定

- 源码与分阶段指标确认原生路径包含额外 D3D11 device/context、ANGLE 设备、双共享纹理和 D3D11VA 解码表面；双 1080p BGRA 纹理本身仅约 15.8 MiB。
- 原生 Private/GPU committed P95 仍为 MediaKit 的约 114.5%/113.8%，未进入 110% 门槛；停止默认替换路线，不继续把 seek 合并、正式状态机和停滞判定下沉到该实验后端。
- 默认 MediaKit、filtered queue、当前 index、标签语义和返回媒体库状态均不改变。
# 2026-07-14 媒体库增删联动播放器十轮压测

- 10 轮共进入播放器 20 次并随机 seek 60 次；诊断样本中视频/音频停滞均为 0，seek 总体 P95 140 ms、最大 154 ms。
- 6/20 样本确认实际硬解为 `no`，对应已知 8K H.264 风险路径；硬解样本 seek 通常为 25–28 ms。
- 线程/Private/GPU committed 峰值分别为 318、2,342 MiB、941 MiB，退出后的高位资源保留仍需后续底层专项；filtered queue 与 PlayerBackend 默认实现未修改。

# 2026-07-14 原生释放屏障与 8K 预检

- 页面退出先等待 stop，再执行 backend dispose；Windows MediaKit 的 `released` 延后到依赖内置的 5 秒 `mpv_terminate_destroy` 宽限期结束，下一会话不再与旧 mpv/D3D 资源重叠。
- 用户点击但媒体规格尚未缓存时，媒体库只对当前项执行独立高优先级预检；播放器页面与 filtered queue 继续只读缓存，不启动 FFprobe。
- 已确认 7680×4320 H.264 回退 CPU 时默认阻止直接播放，保留代理/转码建议。三轮真实回归中两次命中均在纹理创建前阻止，6 个实际播放会话全部硬解，seek P95 28 ms且无音视频停滞。
- 多轮返回媒体库后的 Private/GPU 高位仍未完全回到启动基线，后续应继续在原生 Backend/驱动边界分析，不在页面层强制 GC 或清理 Flutter ImageCache。

# 2026-07-15 队列搜索按需展开

- 队列头部默认不再持续占用一行搜索空间；`当前序号 / 总数` 后提供搜索开关，点击后在标题行下方展开输入框，再次点击收起。
- 搜索、定位已选中和删除入口使用统一的 32×32 点击区域及固定间距，展开搜索后输入框自动获得焦点。
- 搜索提交仍调用既有当前 filtered queue 内定位逻辑，不重新查询媒体库，也不改变播放项、选中项或队列内容。
- 展开/提交/收起与图标顺序 focused test、既有队列搜索语义测试、`flutter analyze` 和 Windows debug build 通过；未执行无关全量测试。
- 真实启动 Windows debug 程序进入播放器，完成默认隐藏、点击展开和再次收起三态截图检查，未见遮挡、溢出、对齐或状态反馈问题。

# 2026-07-15 画面比例与分组播放设置

- 真实媒体详情确认全屏差异不是进度条改变布局：16:9 样本为 `1920×1080`，留边样本为 `1728×1080`（16:10）且源内带上下黑边；默认完整显示必然在 16:9 全屏两侧留边。
- 齿轮入口改为紧凑分组浮层，提供现有播放方式、`自动 / 4:3 / 16:9 / 铺满`、六档倍速、快捷键和播放诊断；不增加不可用占位能力。
- `PlayerBackend.buildVideoSurface` 增加可选 `BoxFit` 和显示宽高比；自动模式继续 `contain`，铺满模式使用 media_kit `cover` 并结合 mpv panscan 等比裁边，切换视频时恢复当前会话选择。
- focused tests 覆盖比例参数映射、设置网格回调、400px Dialog 打开/关闭和原有 filtered queue 播放模式边界；`flutter analyze`、Windows debug build 通过，未执行全量测试。
- 真实窗口已确认自动模式留边、设置分组与选中反馈，并在迭代中发现和修正 MenuAnchor 不显示、Dialog 过宽以及单独 panscan 无法处理外层纹理留边。最终构建复测时自动化进程意外退出，准确人工路径为：启动 debug exe → 第 5 个 1728×1080 视频 → 暂停 → 齿轮 → 确认 400px 浮层 → 全屏 → 自动/铺满往返。

# 2026-07-15 播放设置全局持久化与三级列表

- 一级继续保留镜像、单曲循环和列表循环；更多播放设置删除重复播放方式、快捷键和播放诊断，仅显示视频比例与播放速度当前值及三级列表入口。
- `PlaybackSettings` 向后兼容增加镜像、队列播放方式、比例和倍速；页面变更即时应用，写入串行化，并在退出播放器前等待最后一次持久化完成。
- 每次媒体 open 前后把比例、panscan 和倍速重新应用到 `PlayerBackend`；focused test 明确验证后端收到持久化的 1.5x 与铺满参数，不以 JSON 或 UI 选中态代替真实生效。
- focused 7 项、完整 128 项测试、`flutter analyze`、Windows debug build 和真实窗口一级/二级/三级点击通过，2 项显式基准跳过；完整重启后“铺满 / 1.5x”仍显示并真实生效，验证后恢复默认。filtered queue、当前索引、SQLite、标签语义和缓存队列均未改变。
