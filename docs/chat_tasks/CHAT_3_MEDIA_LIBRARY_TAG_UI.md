# CHAT_3_MEDIA_LIBRARY_TAG_UI.md

## 2026-07-21 卡片改名后的搜索与排序刷新回归

- 真实 `LibraryPage` 页面测试从名称升序网格打开卡片更多菜单并确认改名，隔离临时文件和内存 Repository 共同覆盖物理路径、stable mutable path 与页面刷新链路。
- `charlie → alpha` 后结果立即重排到 `bravo` 前；搜索旧名称得到空结果，搜索新名称只显示改名卡片，证明真实 `TextField` 与页面过滤状态读取最新路径和标题。
- 测试记录 `resultCounts` 调用次数，确认改名和关键字输入不会重新统计未变化的标签；完整 236 项测试、analyze 与 Windows debug build 通过。
- 未修改生产 UI、SQLite schema、`FilterQuery` / `TagQueryService`、filtered queue、缩略图/media 队列或用户数据。

## 2026-07-18 Apple UI Phase 1 排序入口与菜单几何统一

- expanded 排序字段固定为 168px，六项 `PopupMenuButton` 弹层使用同一紧约束，入口与菜单左边缘和宽度一致；medium/compact 图标形态不变。
- 排序方向维持 48px 命中区，动作带调整为 380px 并把少量余量放在排序与多选/视图分组之间，搜索与排序仍保持 12px 紧邻关系，多选切换不改变搜索宽度。
- focused tests 覆盖入口/菜单宽度、展开位置、动作间距和 150% 文字缩放；完整 211 项测试、analyze 和 Windows debug build 通过，3 项 benchmark 跳过。
- `FilterQuery`、`TagQueryService`、排序结果、filtered queue、缩略图队列和用户数据未改变；真实触控板连续截图等用户完成惯性滚动并回复“已空闲”后补拍。

## 2026-07-18 Apple UI Phase 1 媒体库顶部边界与回顶终态修正

- expanded 结果离开 offset 0 后收起完整顶部信息区，中途停留和向上反向均保持隐藏；只有返回绝对顶部并稳定 140ms 才恢复。160/220ms 结构动画仍可中断，避免边界快速反向闪跳。
- 回顶入口以首次结果视口高度为阈值，改用 44px 深色无亮环表面和紫色 `chevron.up`；点击继续按当前距离在 220–520ms 内平滑回顶，隐藏后退出命中、焦点和语义树。
- 滚动监听只在顶部边界或首屏阈值布尔值改变时通知，顶部仍由独立 `ValueNotifier` 驱动；增量结果、缩略图 Future、卡片状态、过滤结果和 filtered queue 不重建或改写。
- focused tests、完整 210 项测试、静态分析和 Windows debug build 通过。1249×714 实窗补拍“日期”六项排序菜单；检测到用户输入后停止滚动与页面点击，保留精确复测路径。

## 2026-07-18 Apple UI Phase 1 排序动作带补齐

- 固定 360px 动作带继续保护普通/多选切换时的搜索宽度，但移除由 `spaceBetween` 产生的大块裸空白；expanded 当前排序字段弹性铺开，后接方向、多选和视图切换。
- 排序字段直接显示当前状态并保留原菜单项、方向、回调、tooltip、键盘和 Semantics；中小布局仍为紧凑图标，过滤与排序语义不变。
- 动作间距、150% 文字缩放和多选宽度 focused tests、完整 205 项测试、analyze、Windows debug build 与 1248×714 实窗截图通过；用户输入检测后未继续抢占菜单点击。
- `FilterQuery`、`TagQueryService`、filtered queue、排序结果、缩略图队列和用户数据未改变。

## 2026-07-18 Apple UI Phase 1 顶部信息区复修

- expanded 顶部不再用大圆角结构表面包裹已经带 surface 的搜索、排序和视图控件，避免双重边框和后台工具台感；中小布局保持原紧凑容器策略。
- 标题使用 24px / 800、正常中文字距和较轻副标题；结果数继续提供即时筛选反馈，标签入口常态中性、活动时紫色，未复制标签计数或筛选状态。
- 搜索到排序保持 12px，右侧动作带固定 360px；多选切换前后搜索宽度稳定，排序、多选与视图入口保留 48px 命中区。
- focused tests 覆盖 expanded 分层、动作间距、批量模式稳定宽度和真实 150% 文字缩放；完整 205 项测试、analyze、Windows debug build 与 1248×714 实窗截图通过。
- `FilterQuery`、`TagQueryService`、filtered queue、排序结果、缩略图队列和用户数据未改变。

## 2026-07-18 Apple UI Phase 1 媒体库主工作区

- 左侧导航以稳定实色侧栏、窄选中指示条和 40px 行高重建浏览层级；右侧标签发现移除紫色硬边、双重阴影和外浮卡感，所有标签层级、排除和父子筛选入口保持。
- 顶部搜索、筛选状态、排序、多选与视图切换进入同一 18px 圆角工具栏；真实 `TextField` 与既有过滤刷新链路不变，内部横向 8px 留白继续保护桌面搜索主层级。
- 视频卡片统一使用 `AppRadius.card` 内容表面，hover / focus / selected / press 反馈遵守 reduced motion 与 high contrast；卡片 State、滚动位置、增量加载、缩略图 Future 和 filtered queue 均不重建。
- 文字缩放进入卡片可见几何：100%/125%/150% 的标题槽位分别为 42/50/58px，并增加高 DPI 舍入余量；新增 150% 双行标题实体测试。
- 最新 Debug EXE 已完成 100% 的搜索/清除、标签展开/父子选择/清空、打开视频/返回连续截图；返回后进程保持且 11,163 条结果恢复。125%/150% 结果状态按文字倍率增加桌面宽度预算后完整显示五位数数量，卡片双行标题、工具栏与侧栏无裁切或溢出。
- 完整 198 项测试通过，3 项 benchmark 跳过；analyze 和 Windows debug build 通过。
- SQLite、过滤语义、filtered queue、PlayerBackend、缓存队列、稳定身份和用户数据未改变。

## 2026-07-18 Apple UI Phase 1 搜索与筛选状态组件族

- 顶部继续使用唯一真实 `TextField`、controller、FocusNode 与统一 `onChanged` 链路；搜索表面新增真实 hover/focus 轮廓、reduced motion 与 high contrast 降级，不改变筛选刷新时序。
- 搜索和筛选状态复用 Phase 0 的圆角、间距、排版、动效与 `AppInteractionSurface`；关键词清除和清空筛选拥有 40px 命中区、tooltip、键盘焦点和 Semantics。
- 筛选状态建立低对比度实色层级，空筛选显示“全部视频”，活动 chip 常态保持中性；结果数使用强调点与主要文字，并为后台刷新提供 live-region 语义。
- 4 项 focused tests、完整 193 项测试通过，3 项 benchmark 跳过；`flutter analyze` 和 Windows debug build 通过。真实 1249×714、11,163 条媒体窗口确认顶部对齐、焦点轮廓和首屏结果无遮挡/溢出；连续输入/标签点击因检测到用户操作窗口而停止抢占并保留精确复测路径。
- 未修改 SQLite schema、`FilterQuery` / `TagQueryService`、filtered queue、排序、缩略图/media 队列或用户数据。

## 2026-07-17 增强左右侧栏结构动画

- 左右侧栏开合时长从 260ms 调整为 320ms，内容统一组合更明显的横向位移、淡入和轻微缩放，移动边缘同步过渡描边与阴影。
- 仍沿用现有稳定列数基准和 `AnimatedSwitcher` 子树身份，不触发筛选、计数、滚动、缩略图或播放队列重算。
- 新增 focused widget；完整 166 项测试、analyze 和 Windows debug build 通过。最新 Debug 窗口完成左右栏往返开合及快速反向截图，三列稳定且无裁切、闪白或溢出。

## 2026-07-17 标签面板标题统一折叠入口

- 删除展开面板右上角独立收起箭头；“筛选图标 + 标签筛选”标题区域改为 48px 高的收起入口，折叠窄条继续负责展开。
- 标题按钮具备 tooltip、键盘焦点和辅助技术语义；dense 弹层保持纯标题，不创建无效动作。
- focused widget 覆盖标题点击与箭头移除；1249×714 真实窗口完成展开、标题收起、再次展开截图，布局与网格列数稳定。
- 完整 157 项测试、`flutter analyze` 和 Windows debug build 通过。

## 2026-07-17 侧栏开合固定媒体网格列数

- 网格列数使用窗口宽度扣除默认侧栏占位后的稳定基准；窗口大小不变时，左侧功能栏与右侧标签筛选开合只缩放卡片，不再改变列数。
- 窗口拖动仍保留稳定后跨越响应式断点的能力；筛选、计数、滚动、缩略图和完整播放队列均不受影响。
- focused widget 覆盖侧栏宽度变化结束后保持三列；1268×714 真实窗口完成左右侧栏四种组合点击与截图，均保持三列且无溢出或遮挡。
- 完整 156 项测试、`flutter analyze` 和 Windows debug build 通过。

## 2026-07-16 侧栏动画连续布局修正

- 替换旧宽度网格裁切方案：侧栏动画期间锁定列数，但卡片尺寸按当前结果区宽度连续变化，不再出现收起留白或展开裁切。
- 约束稳定后才跨越列数断点；卡片 State、滚动位置、筛选结果、缩略图 Future 和完整播放队列均保持不变。
- 左侧栏展开内容与折叠轨道使用各自目标宽度淡入淡出，移除中途宽度阈值触发的整树突变，并覆盖退场期间无 overflow。
- 完整 156 项测试、analyze、Windows build 通过，新 Debug EXE 响应正常；当前无 `computer-use` Windows 控制入口，真实左右栏连续点击与截图保留精确人工复测路径。

## 2026-07-16 卡片对齐与侧栏重排缓冲

- 顶栏底部留白扩大为 18px，首行缩略图不再紧贴搜索和筛选控件。
- 卡片标题固定使用 42px 两行内容槽位，短标题和长标题共享同一卡片高度与下一行起点。
- 网格在左右栏动画期间锁定最近稳定宽度，停止后只重排一次；不触发筛选、计数、缩略图重新请求或播放队列变化。
- focused widget 覆盖固定标题高度、顶部留白、侧栏入口和宽度缓冲时序；完整 156 项测试、analyze、Windows build 通过。
- Debug EXE 已启动且响应正常；当前无 `computer-use` Windows 控制执行入口，真实左右栏点击与截图保留精确人工复测路径。

## 2026-07-16 顶栏视觉权重再平衡

- 宽屏搜索、媒体库状态和操作区调整为 50/40/10；搜索框缩短约 17%，标签浏览获得更稳定的横向空间。
- 状态阅读顺序改为“标签 → 排序 → 视频数量”，数量不再贴近搜索输入；后台进度继续占用同一结果状态位置。
- 多选改为 48px 高的透明无边框弱文字入口，常态使用次级文字色，交互时才增强反馈。
- focused widget 覆盖比例、控件顺序、弱按钮样式、medium 单行和多选切换。
- 154 项完整测试、analyze、Windows build 通过，最新 Debug EXE 响应正常；当前无 `computer-use` 专用 Windows 控制入口，保留精确人工截图路径。

## 2026-07-16 搜索与筛选状态分区

- 单层顶栏内部拆分为搜索、筛选状态和操作三个独立区域；宽屏使用 60/30/10 主视觉比例，搜索框不再承载 chips 或结果数量。
- 标签状态使用透明承载区和中性灰 Chip，常态无紫色描边；浅紫只用于 hover、focus、press 状态变化。
- 多选模式只替换搜索框右侧区域，搜索输入保持可见且宽度不变；排序保留为紧凑状态控制，多选与视图滑块留在末端操作区。
- focused widget 覆盖两个标签显示、区域比例、Chip 颜色/描边、多选替换边界和 medium 单行无 overflow。
- 154 项完整测试、analyze、Windows build 通过，最新 Debug EXE 响应正常；当前会话无 `computer-use` 专用 Windows 控制入口，真实点击与截图留待按记录路径复测。

## 2026-07-16 顶栏输入保护与连续视图滑块

- chips 使用搜索输入之外的剩余宽度，expanded `TextField` 至少保留 236px；两个标签可同时显示，更多条件优先折叠为数量。
- 多选入口统一为相邻工具控件的 48px 高度、10px 圆角、深色表面和描边。
- 网格/列表滑块先独立完成连续位移，再提交结果区重布局；支持动画中快速反向，并抵抗无关父级重建。
- 154 项测试通过，3 项显式 benchmark 跳过；format、analyze、Windows build 通过，Debug EXE 响应正常。当前 `computer-use` 控制通道不可用，保留精确人工点击与截图路径。

## 2026-07-16 单层搜索筛选工具栏

- 删除视频区域上方独立筛选行；标签、收藏和排除条件作为可移除 chip 进入真实搜索表面，关键词仍由稳定 `TextField` 链路驱动，结果数量显示在搜索区域右侧。
- 搜索表面宽屏最大约 650px，右侧同层保留排序、多选和视图切换；medium 极窄结果区使用紧凑排序控件并通过 452px 无 overflow 测试。
- 标签中心移入左侧功能栏，折叠态仍可访问；进入多选后整条顶部替换为选择状态、删除和取消，退出时恢复原筛选状态。
- 153 项测试通过，3 项显式 benchmark 跳过；format、analyze、Windows build 通过，Debug EXE 响应正常。当前 Windows 点击/截图通道不可用，保留精确人工复测路径。

## 2026-07-16 网格与列表单体滑块

- 结果视图切换改为单一点击区域，每次点击在网格和列表之间翻转，不再分别点击左右图标。
- 选中底块使用 180ms 动画滑向当前布局图标，图标只显示状态；95 项 widget、analyze、Windows build 通过，Debug EXE 响应正常。

## 2026-07-16 筛选与批量操作双状态工具栏

- 普通状态压缩为 64px 单行工具栏，只展示筛选标签、折叠数量、清空、视频数量和多选入口；不再重复展示“当前筛选（AND）”和查询摘要。
- 多选状态整行切换为全选、选择计数、删除和取消。卡片左上角使用圆形复选框替换收藏红心，点击卡片只选择，不播放或启动动态预览。
- 批量删除继续使用既有回收站确认和平台边界，稳定 `videoId` 选择可跨当前增量挂载范围工作；95 项 widget、analyze、Windows build 通过，Debug EXE 响应正常。

## 2026-07-16 侧栏品牌折叠入口

- 主功能栏隐藏桌面纵向滚动条，但滚轮、触控板和鼠标拖拽仍可滚动；展开、折叠和本地 root 列表共用同一行为。
- 删除两套独立双箭头，紫色品牌图标承担唯一折叠入口；展开时三角向右，折叠时三角向下。
- 93 项 widget、analyze、Windows build 通过；新 Debug EXE 响应正常，真实点击和截图因当前会话无 Windows 自动控制入口留待精确路径复测。

## 2026-07-16 卡片悬停更多菜单

- 网格卡片标题右侧新增竖向更多按钮，默认透明且不可命中；卡片 hover、键盘焦点或菜单展开期间淡入，移开且失焦后恢复隐藏。
- 标题固定预留按钮槽位，显隐不改变标题宽度；菜单提供“编辑标签”和“删除文件”，分别复用统一标签编辑器和带回收站选项的删除确认流程。
- 主媒体库和本地目录网格均接入；93 项 widget、analyze、Windows build 通过。Debug EXE 响应正常，但当前会话无 Windows 自动点击接口，保留真实悬停与菜单截图复测路径。

## 2026-07-16 动态预览时长与透明收藏叠层

- 动态预览真正显示后，时长角标以 180ms 淡出；移开时与静态缩略图同步恢复，加载阶段仍显示时长。
- 收藏红心移除常态和交互态底色，仅保留轮廓/实心颜色与阴影，点击范围和收藏逻辑不变。
- 92 项 widget、analyze、Windows build 通过；当前 Windows 自动化入口不可用，保留动态预览时长和透明红心的人工截图复测路径。

## 2026-07-16 缩略图动态放大与默认布局扩容

- hover 改为缩略图裁剪框内 `1.06x` 连续缩放，标题和角标保持原位；220ms 进入、170ms 退出可从当前动画进度反向衔接。
- 标签筛选折叠条横向占用从约 92px 缩至 64px，网格内边距、列间距和卡片最大宽度阈值同步调整，默认布局把新增空间用于放大卡片。
- 92 项 widget、analyze、Windows build 通过；当前工具列表未暴露 Windows 自动化入口，保留快速横扫、单卡停留/移出和折叠条宽度的人工截图复测路径。

## 2026-07-16 动态预览延迟与退出淡出

- 动态预览改为连续停留 650ms 后启动，离开前会取消尚未触发的意图，快速横扫卡片不再出现加载闪动。
- 已显示的动态画面用 180ms 淡回静态缩略图后释放播放器；淡出期间重新移入直接复用已有预览。
- 92 项 widget、analyze、Windows build 通过；1268px 真实窗口完成快速横扫、稳定停留、退出复位和淡出期间重入验证。

## 2026-07-16 深色缩略图占位与局部悬停浮动

- 加载中、生成失败和无缩略图共用深色渐变占位视觉，状态含义由进度环或图标区分；悬停动态预览加载层不再出现浅色圆形区域。
- 卡片 hover 不再绘制整卡紫色边框和背景，只让缩略图上浮 3px 并增加轻量阴影；标题位置、点击播放和 filtered queue 均保持不变。
- 90 项 widget、analyze、Windows build 通过；1268px 真实窗口确认缩略图局部浮动、动态预览启动和标题不位移，三种占位状态由组件测试覆盖。

## 2026-07-16 收藏与时长叠层微调

- 收藏按钮、红心尺寸和边缘间距按卡片宽度分为三档；底色透明度降到 46%，使用图标阴影维持高亮缩略图上的轮廓。
- 时长角标字号和内边距同步三档化，底色透明度降到 56%，以 600 字重和轻量阴影保持可读性。
- 1268px 真实窗口确认未收藏/已收藏及亮暗缩略图的叠层视觉；收藏切换成功并在用户窗口空闲后恢复原状态，收藏总数回到 5。

## 2026-07-16 响应式标题与网格密度

- 标题按卡片宽度分为 13.5/14.5/15.5/16px，使用 600 字重与稳定两行高度；缩略图、悬停外框和点击反馈统一为 8px 圆角。
- 网格横向间距按宽度使用 10/14/18/22px，超宽结果区提高单卡最大宽度；2200px 结果区固定为 5 列，避免高分辨率下形成密集小卡片墙。
- 1268px 真实窗口三列复测通过；宽屏桌面输入因用户正在操作其它窗口而停止，响应式列数、间距、字号和高度使用纯函数测试替代覆盖。

## 2026-07-16 深色信息流卡片与可折叠主功能栏

- 媒体库左侧功能栏支持折叠为 76px 图标轨道，全部入口保留 tooltip 和原回调；折叠只改变布局宽度，不修改筛选状态、滚动范围或 filtered queue。
- 媒体库工作区统一为局部深色主题，搜索、摘要、排序、标签面板、背景和空状态不再形成浅色孤岛；其它页面仍沿用原全局主题。
- 网格卡片采用 16:9 缩略图加两行标题的信息流结构，常态无浅色外壳和重阴影，悬停才显示轻量表面与紫色边框。
- 88 项 widget、analyze、Windows build 通过；真实 11,163 条媒体库完成折叠/展开、标签面板、播放器进入/返回复测，无抖动、溢出或状态丢失。

## 2026-07-16 提前预加载与筛选缩略图稳定复用

- 当前批次剩余 4 行时提前追加下一批 10 行；Sliver 仍只构建视口与 cacheExtent 附近项目，连续追加数百行不会把全部卡片常驻内存。
- 已挂载范围缓存稳定 `videoId -> index`，筛选后同一视频跨下标移动可复用卡片 State；进程内已验证缩略图直接作为首帧，避免标签筛选时闪回加载占位。
- debug 下可用 `LOCAL_TAG_PLAYER_SCROLL_STATS_OUTPUT` 单独启用轻量 Flutter 帧统计，滚动结束后输出 P50/P95/峰值与超预算帧，不启用原有重型卡片 build/layout 探针。
- 88 项 widget、analyze、Windows build 通过。真实 11,163 条媒体库连续追加到 450 张卡片（约 150 行）无回顶、闪白或错位；轻量统计拆分后的第二轮因用户输入保护中止，保留精确人工复测路径。

## 2026-07-16 十行增量滚动与标签自动折叠

- 移除页码与翻页按钮；网格每批追加当前列数 × 10 个卡片，列表每批追加 10 条，完整筛选结果只用于按索引构建和播放器队列。
- 单一滚动控制器在距离批次末尾约两行时调度追加，同一帧合并重复通知；已有 item key、滚动偏移和缩略图 Future 保持稳定。
- 标签筛选默认折叠，展开后选择一级、二级、分组、排除或收藏筛选都会自动恢复窄条；搜索输入和顶部 chip 清理不改变面板状态。
- 87 项 widget、analyze、Windows build 通过。真实 11,163 条窗口确认无分页栏、默认窄条和滚动追加无回顶/闪白；标签点击自动截图因用户输入保护停止，保留精确人工复测路径。

## 2026-07-16 分页文案精简与可见时长刷新

- 分页栏只保留“第 X / Y 页”，不再显示当前范围和总结果数；四个翻页按钮及每页 100 条保持不变。
- 可见卡片的旧详情缺少总时长时进入既有优先探测队列；批次完成后轻量刷新当前视图，不提升列表 revision 或重新计算标签计数。
- 真实新构建窗口确认“第 1 / 112 页”和多组 `M:SS` 时长同时可见，分页条与卡片角标均无遮挡、截断或错位。

## 2026-07-16 媒体库每页 100 条分页

- 网格与列表模式统一按每页 100 条切片展示，超过一页时在结果区底部显示首页、上一页、下一页、末页和精确范围。
- 翻页不重新筛选、排序或刷新标签计数；每页滚动位置从顶部开始，筛选/排序结果边界变化时回到第一页。
- 播放入口继续接收未分页的完整 filtered queue。205 条 widget 回归确认从第二页打开时队列仍为 205 条；真实 11,163 条媒体库的首页、第二页、末页点击与截图验证通过。

## 2026-07-16 紧凑视频卡片

- 网格卡片只保留缩略图与两行标题，移除播放按钮、底部操作、标签和路径；收藏/时长分别放到缩略图左上角和右下角。
- 卡片整体接管原播放入口，仍把当前可见结果作为 filtered queue 传入播放器；列表模式的播放/收藏/更多操作保持不变。
- 卡片高度按实际列宽计算，真实 1268×714 三列窗口约 168px；点击收藏后反馈明确，恢复收藏并点击卡片进入播放器/返回均通过。

## 2026-07-15 空媒体库文件导入与目录删除入口

- 空库结果区新增方框大“+”和添加视频提示，点击使用平台多文件选择契约；筛选无结果不会错误显示导入入口。
- 媒体库结果区接收桌面文件/目录拖放并显示悬停覆盖反馈；文件父目录与拖入目录按最上层 root 归并，同批只执行一轮扫描。
- 目录管理删除复用左侧 root 统一清理协调，不删除磁盘文件。完整 115 项测试、analyze、Windows build 与隔离真实窗口选择文件入库截图通过，2 项基准跳过；跨窗口拖放受自动化边界限制，需人工补充最终释放复测。

## 2026-07-13 扫描差量帧耗时对照

- debug 真实窗口扫描使用固定 3 秒 Flutter 帧驱动，按 scan/postApply 分开记录 build、raster、total 的 P95/峰值与超预算帧。
- 11,133 条大量差量中 folder 侧边栏全量派生约 102 ms，是 postApply 最大的已命名 UI 阶段，但仍低于 Application 合并/SQLite 提交造成的整体峰值。
- 零差量不再提升 library revision、失效 folder 缓存或重算 filter；两个阶段均为未执行。
- 像素截图中可见缩略图、右侧 folder 标签列表和扫描结果 SnackBar 无遮挡、溢出或状态错位。

## 2026-07-12 搜索结果闭环 P1 修复

- 顶部结果摘要改为实际选择名称、关键词和即时命中数，例如“原神 / 雷神 + 关键词 raiden · 41 个结果”；不等待标签旁计数重算。
- `Ctrl+K` 除原 Shortcuts 链外增加页面生命周期键盘处理，并限制为媒体库路由位于最上层时生效。
- 真实窗口使用物理按键逐字输入 `raiden`，确认搜索文字可见、卡片与 41 个结果同步更新。

当前版本：`0.2.41`
状态：已完成
负责人：Chat 3 / 媒体库标签 UI

## 规划来源

主要来源：

```text
<private-planning-document>
```

如果本文档与该文件冲突，以外部规划为准。

## 范围

负责媒体库首页的标签发现 UI。这是功能 UI，不是最终视觉 polish。

允许：

- `LibraryPage` 中标签发现所需布局。
- 筛选侧栏。
- 筛选条。
- 标签 chip。
- 结果数量 UI。
- 清空筛选 UI。
- 保存智能列表入口 UI。
- 第一轮 `expanded`、`medium`、`compact` 响应式行为。

禁止：

- SQLite schema 修改。
- 核心标签查询行为。
- 播放器 backend 或 FFmpeg 内部逻辑。
- 与标签发现无关的大范围最终视觉 polish。

## P0 任务

- 把首页从扁平标签升级为网页式分组筛选 UI。
- 左侧：分组标签筛选。
- 顶部：搜索文件名 / 路径 / 标签 / 别名。
- 中上：当前筛选 chips。
- 展示当前结果数量和总数量。
- 支持清空筛选。
- 支持排除标签 UI，例如 `[-NTR x]`。
- 增加保存当前筛选为智能列表入口。
- 确保点击播放仍进入当前筛选播放队列。

## 变更记录

### `0.2.29` 十行增量滚动与标签自动折叠

- 页码切换替换为同一结果视图按 10 行触底追加。
- 尾部追加保持现有滚动位置和卡片身份，完整 filtered queue 不变。
- 标签面板默认折叠，真实标签选择后自动收起。

### `0.2.28` 分页文案精简与可见时长刷新

- 分页栏移除条目范围，只保留当前页 / 总页数。
- 可见旧详情按既有优先队列补齐总时长，完成后轻量刷新卡片。
- 筛选、排序、标签计数和完整 filtered queue 均保持不变。

### `0.2.27` 媒体库每页 100 条分页

- 网格和列表共享固定 100 条分页及紧凑底部分页条。
- 分页仅影响展示切片，不截断播放器的当前筛选队列。
- 筛选/排序结果边界变化后回到第一页，翻页后的列表从页首显示。

### `0.2.24` 扫描帧耗时与零差量快路径

- 扫描诊断持续采样帧耗时并单独记录 folder 侧边栏重算与 filter 差量替换。
- 零差量扫描仅更新轻量状态与 SnackBar，不重建媒体列表或右侧 folder 树。

### `0.2.23` 一级/二级标签页签和排序响应

- 右侧标签筛选的“一级标签”页签只展示 `folder.primary` 文件夹一级标签，不再在同一页签底部混入热门二级标签。
- 一级候选额外校验 `folder` 来源、无父级和 `folder.primary:*` id 形态；历史污染数据中误入 `folder.primary` 组的二级/manual 标签不会出现在一级列表。
- 二级候选额外校验 `folder.child`、`folder` 来源、带父级和 `folder.child:*` id 形态，继续只在一级展开卡或“全部二级标签”页签展示。
- folder 一级/二级候选最终以真实视频路径和本地媒体库 root 重新派生；多个 root 命中时优先使用最上层 root，保证 `X:\test-media\崩坏三` 是一级、`X:\test-media\崩坏三\李素裳` 是二级。
- 二级标签筛选也使用同一文件树层级：`FilterQuery` 携带当前媒体库 roots，按最上层 root 重新派生层级后匹配视频，避免子 root 历史扫描导致二级标签筛选结果为 0。
- “全部二级标签”继续展示完整 `folder.child` 候选，一级展开卡内仍可查看当前一级下的二级标签。
- 排序字段和方向切换改为直接重排当前结果，不触发完整筛选刷新和标签计数重算，降低切换排序时的卡顿。
- 排序与标签筛选交互优先保证界面流畅：排序预计算自然排序 token 和路径 key，筛选后的结果排序复用同一入口，标签计数延后到可见结果更新后刷新。
- “添加时间”排序只使用 `addedAt`，不再受播放器返回时更新 `lastPlayedAt` 影响。
- 排序字段和方向保存到独立 `library_sort.json`；全量媒体库、标签筛选、本地收藏和最近播放统一使用同一排序 helper。
- 排序字段进一步对齐 Windows 文件排序：菜单展示“名称 / 日期 / 类型 / 大小 / 目录 / 添加时间”；“日期”优先按文件修改时间排序并兼容旧 `recent` 偏好 key，名称使用大小写不敏感自然排序。
- 本地媒体库路径浏览的视频项也使用同一排序 helper；文件夹继续显示在视频前面。
- 新增 widget smoke 覆盖一级页签不显示二级标签云、默认排序立即生效和全部二级页签展示完整二级标签。
- 本轮未修改 SQLite schema、`FilterQuery` / `TagQueryService` 查询语义、播放器 filtered queue 或缩略图/media 队列。

### `0.2.22` 二级标签面板修复

- 热门二级标签使用真实“更多 / 收起”按钮，不再是禁用态视觉按钮；按钮可展开热门列表，并显示当前可见数量 / 总数量。
- “全部二级标签”页签接收完整二级标签列表，不再复用热门列表切片。
- 热门二级 chip 默认保持轻量；重名标签显示所属一级标签，让 `NTR/ntr` 等歧义名称可区分。
- 复查媒体库和本地媒体库入口语义：媒体库重置为全部视频，本地媒体库打开已配置 root 或子目录的文件路径浏览。
- 验证通过：`dart format`、`flutter test`（18 项）、`flutter analyze`、`flutter build windows --debug`，并用 computer-use smoke 覆盖更多标签、全部二级标签、媒体库重置和本地媒体库浏览。

### `0.2.21` 非最大化窗口可视性修复

- 普通 debug 窗口下，expanded 顶部搜索框可随实际行宽收缩，避免被右侧标签面板挤出。
- 提高单列视频卡片高度，让 16:9 缩略图、标题、标签和底部操作不出现 Flutter overflow 条纹。
- 列表行动作在中等宽度下切换为紧凑图标按钮，保证播放 / 收藏 / 更多不越界。
- computer-use 覆盖普通窗口和最大化窗口：顶部工具条、列表行动作、右侧标签面板和本地媒体库返回入口都保持在可视范围内。
- 验证通过：`dart format`、`flutter test`（16 项）、`flutter analyze`、`flutter build windows --debug`。

### `0.2.20` 列表行与结果状态 smoke

- 扩展稳定 smoke harness，覆盖密集列表模式下的本地文件夹导航、列表行播放 / 收藏 / 更多控件，以及右侧标签结果状态断言。
- 为列表行控件和标签 chip 增加稳定 key；列表行 smoke test 会等待超过行双击识别窗口，让单击按钮断言保持确定性。
- 右侧标签 harness 增加小型结果状态区：选择默认专辑显示当前一级下全部示例结果，选择 Child01 则收敛到 Child01。
- 验证通过：`dart format`、`flutter test`（16 项）、`flutter analyze`、`flutter build windows --debug`。

### `0.2.19` 稳定 smoke harness

- 将最高风险的媒体库点击 smoke test 从截图坐标改为稳定 widget key 和测试宿主。
- 新增 `LibrarySmokeKeys`、`LocalLibrarySmokeHarness` 和 `TagDiscoverySmokeHarness`，让测试可驱动真实本地媒体库文件夹视图和右侧标签发现面板，而不打开真实数据库，也不依赖桌面 DPI。
- Widget smoke 覆盖本地文件夹打开、顶部返回按钮、鼠标后退侧键、一级标签展开/收起、二级“展开全部 / 收起”和一级/二级页签切换。
- 验证通过：`dart format`、`flutter test`、`flutter analyze`、`flutter build windows --debug`；debug exe 可用临时 profile 启动并保持运行。

### `0.2.18` 本地文件夹返回与二级标签清理

- 新增 `LOCAL_TAG_PLAYER_DATA_DIR` 临时 profile 覆盖能力，用于可回滚 UI smoke test；未设置时真实应用数据路径不变。
- 临时 profile 鼠标 QA 覆盖最近播放单条删除、全选删除、清空全部；每条路径只清理临时库里的 `lastPlayedAt`。
- 临时 profile 下记录入口切换耗时：媒体库首次约 213ms，最近播放约 63ms，智能收藏约 59ms，返回媒体库约 51ms。
- 热门二级标签只显示二级标签名；所属一级小标签在热门区隐藏，但全部二级页签仍可显示。
- 二级候选过滤真实“默认专辑”标签，避免它出现在热门 / 全部二级候选中。
- 一级展开卡过滤真实子标签里的“默认专辑”，只保留卡片开头的虚拟默认专辑 chip。
- 本地媒体库文件夹浏览新增路径返回栈：点击文件夹压入当前路径，顶部返回按钮和鼠标后退侧键都回到上一层。
- 最近播放清理补充单选删除和批量清空目标测试，确认未播放视频不受影响。

### `0.2.17` 主来源与本地媒体库

- 媒体库、最近播放和智能收藏作为轻量主结果来源：最近播放 / 收藏使用内存列表，媒体库清空筛选并立即重建全量视频状态。
- 最近播放主结果区增加管理工具条：单选、全选、删除已选、清空全部；这些动作只清理 `lastPlayedAt`，不删除视频文件、标签、收藏或播放进度。
- “我的标签库”改名为“本地媒体库”，添加 / 移除入口只管理本地 root 路径，不再打开标签新增弹窗。
- 本地媒体库 root 浏览中，文件夹显示为文件夹条目，已索引视频显示为普通视频卡片 / 列表行；既有网格 / 列表切换继续控制布局。
- 设置从顶部工具条移除，保留在左侧功能栏底部；debug exe smoke 确认底部入口可打开已有设置页。

### `0.2.16` 主入口行为

- 最近播放改为主结果模式，不再是弹窗；中间网格 / 列表展示最近播放视频，并把可见列表传给播放器队列。
- 媒体库侧栏项作为重置入口：清空搜索、一级 / 二级 / 分组 / 排除 / 收藏筛选，返回全部视频。
- 设置从顶部工具条和左侧侧栏暴露，复用已有缓存 / 播放设置页。
- “我的标签库”新增弹窗在输入时过滤已有标签，并通过 SnackBar 报告创建 / 保存错误。
- 从最近播放打开视频时使用“最近播放”队列标题，避免播放器显示误导性的全库筛选摘要。

### `0.2.15` 侧栏与列表 QA

- 左侧侧栏收敛到仍服务媒体库闭环的入口：删除重复“播放历史”、低价值“当前筛选”和“常用标签”。
- “最近播放”从侧栏直接打开最近视频入口。
- 目录管理增加非破坏性 root 移除入口，只更新配置 root，不删除磁盘文件或清理已索引视频。
- “我的标签库”曾作为可滚动快捷标签列表，支持添加 / 移除；移除只删除快捷入口，不删除真实 tag 或视频关系。
- 右侧标签数量使用全库稳定计数缓存，选择一级 / 二级标签后无关标签数量仍可见。
- 顶部工具条搜索框不再占满 expanded 宽度，宽屏下标签中心、收藏筛选、排序和网格 / 列表控件保持可见。
- 密集列表模式经真实窗口 QA 后收紧：行内容宽度可读，行高略增，播放 / 收藏 / 更多按钮可见且无 overflow。
- 验证通过：`dart format`、`flutter analyze`、`flutter test`、`flutter build windows --debug`；真实窗口 QA 确认最近播放、目录管理、工具条可见性和列表无 overflow。

### `0.2.5` 参考布局像素调整

- 顶部搜索框增加桌面最大宽度，避免超宽窗口横向拉满。
- 当前筛选行和右侧标签筛选面板保持参考图两行归属：工具条横跨内容区，下面再分为中心筛选 / 结果和右侧标签卡片。
- 右侧标签筛选面板成为独立卡片，外边距、固定桌面宽度、嵌套组弱化和 segmented tabs 更接近参考图。
- 交互 smoke 覆盖收藏筛选、排序菜单、网格 / 列表切换、右侧页签切换、标签 chip 选择和筛选 chip 移除。

### 早期版本摘要

- `0.2.0` 到 `0.2.4`：完成媒体库标签发现主结构、当前筛选条、右侧标签面板、参考图第一轮视觉对齐和基础点击 smoke。
- `0.1.0`：创建媒体库标签 UI 任务模板。

### `0.2.22` 主界面信息架构复修

- 保留视频卡片和全部业务入口，主结果外壳重排为页面标题、搜索操作、活动筛选三层，避免搜索、数量、排序、视图和多选继续挤在单个后台式工具条中。
- 左栏按浏览、资料库和本地 root 重组；选中态改为克制紫色胶囊，固定假进度替换为真实 root、标签和分组统计。
- 右侧收起入口从竖排窄条迁到标题区横向“标签”按钮；展开内容、folder 父子层级、计数、回调和收起动画保持，关闭后结果区释放全部占位。
- 搜索仍为唯一真实 `TextField` 链路；排序、多选、网格/列表、扫描进度、清空筛选、目录动作和设置均继续可达。
- 完整 202 项测试、`flutter analyze`、Windows debug build 通过；1248×714 真实窗口完成标签开合及 100%/125%/150% 文字缩放复核。

## 新对话提示

```text
这是 Chat 3 / 媒体库标签 UI。项目路径：<project-root>。
请先阅读：
- PROJECT.md
- CURRENT_TASK.md
- docs/chat_tasks/CHAT_3_MEDIA_LIBRARY_TAG_UI.md
- 与 LibraryPage / library_widgets 直接相关的源码

目标：维护媒体库首页的标签发现 UI、筛选 chips、结果数量、右侧标签面板和响应式布局。
禁止：不改 SQLite schema、不改 FilterQuery / TagQueryService 语义、不改播放器 backend、不做无关视觉重构。
修改代码后运行：
- dart format
- flutter analyze
- flutter build windows --debug
UI 行为变更还要做 widget smoke 或 computer-use / debug exe 点击验证。
```
