# CHAT_7_RESPONSIVE_UI.md

当前版本：`0.3.0`
状态：第一阶段验收完成；Apple UI Phase 0–2 已完成，Phase 3 标签中心与设置入口已完成代码验证
负责人：Chat 7 / 响应式 UI + 平台 polish

## 2026-07-18 Apple UI Phase 3 设置入口

- 设置首页继续只负责导航，播放与数据两组改为稳定实色面板；四个入口复用 `AppInteractionSurface` 的 hover/focus/press、reduced motion 与 high contrast 反馈，当前继续观看策略仍直接可见。
- 设置二级内容卡片收敛为共享 14px 内容圆角，不改变开关、滑杆、下拉确认、快捷键交换、备份、缓存统计或返回语义。
- focused tests 覆盖四入口可达、共享交互表面和卡片圆角；完整 200 项测试、静态分析和 Windows debug build 通过。真实窗口待用户前台空闲后补验。

## 2026-07-18 Apple UI Phase 0

- `app_theme_tokens.dart` 增加颜色、材质、圆角、间距、排版、阴影和语义动效层级，并把全局浅色 `ThemeData` 收口为可测试构建函数。
- `MaterialApp` 从 `MediaQuery` 派生 `disableAnimations`、`accessibleNavigation`、`highContrast` 与系统文字缩放策略，不覆盖用户的 text scaler。
- 新增 `AppInteractionSurface`，统一 press、hover、focus 和键盘激活反馈；保留 `InkWell` 与 Semantics，reduced motion 下停用缩放，高对比度下透明材质回退实色。
- 本阶段没有批量替换页面组件，没有启用大面积 blur，也没有修改页面信息架构或任何过滤、队列、缓存、稳定身份语义。
- 4 项 focused tests、完整 189 项测试、`flutter analyze` 和 Windows debug build 通过；真实最大化窗口完成“媒体库 → 设置 → 返回媒体库”点击与截图，11,163 条媒体库状态保持。
- 下一步从 Phase 1 的一个媒体库组件族开始，先统一搜索/筛选状态或卡片交互表面，再做整页视觉迁移。

## 2026-07-18 Apple 式全应用 UI 第二阶段规划

- 新增 `$ltp-apple-ui-design`，收敛视觉基础、动效决策、审查、机会筛选和术语，并转译为 Flutter/Windows 与大媒体库性能规则。
- 新增 `docs/design/APPLE_UI_MIGRATION.md`，按 Phase 0-5 分阶段迁移共享 token、媒体库、播放器、维护页、全局细节组件和跨平台体验。
- 第二阶段坚持内容优先、克制材质、即时反馈、空间连续和无障碍降级；不复制 macOS、不全局堆叠 blur、不把标签筛选或播放关键路径交给装饰动画。
- 该规划已由 Phase 0 落地；Phase 1 继续遵守同一 token、无障碍和大媒体库性能门槛。

## 规划来源

主要来源：

```text
<private-planning-document>
```

如果本文档与该文件冲突，以外部规划为准。

## 范围

在核心标签发现 UI 可用后，负责最终视觉一致性、响应式布局补全、平台 polish 和 macOS / Linux 适配说明。

允许：

- 卡片、按钮、弹窗、侧栏的视觉一致性。
- 浅色媒体库风格和深色播放器风格的一致性。
- 完整 `compact`、`medium`、`expanded` 布局行为。
- 桌面端平台 polish 说明。
- Architecture 定义边界后的共享 UI token。

禁止：

- 为等这个阶段而延迟标签发现 UI。
- SQLite schema 修改。
- 核心标签查询行为修改。
- 播放器 backend 或 FFmpeg 内部逻辑修改。
- 当前阶段深度适配 Mobile / Web。

## P1 / P2 任务

- 统一视频卡片。
- 统一按钮。
- 统一弹窗。
- 统一侧栏。
- 保持媒体库和播放器视觉一致。
- 完成响应式布局：
  - `expanded`：常驻筛选 / 侧栏布局。
  - `medium`：可折叠侧栏和侧边 sheet。
  - `compact`：drawer / bottom sheet 筛选和紧凑视频列表 / 卡片布局。
- Windows 稳定后增加 macOS / Linux 适配说明。

## 第一阶段结果

`0.2.18` 已完成：

- 标签中心、设置、缓存和缺失重关联页面统一为媒体库深色维护主题；标签分组 chip 提供选中勾选、强调色和列表筛选反馈。
- 设置首页显示当前继续观看策略，二级页把策略下拉提升为首项；网格/列表偏好跨进程恢复。
- 超宽列表增加独立标签与缓存媒体信息列，复用内存数据提高横向密度；不触发全列表标签重算或磁盘探测。
- 添加目录/视频和 Relink 原生选择器从业务相关路径打开；完整 185 项测试、analyze、Windows debug build 和真实最大化窗口截图通过。

`0.2.17` 已完成：

- 继续观看清除提供 10 秒精确撤销，清空全部增加“只清除进度、不删除视频”的二次确认；重播后的新进度不会被撤销覆盖。
- 顶栏在当前宽度下优先展示活动筛选 chip 与清除入口；扫描状态显示阶段、进度、ETA 和取消；本地 root 统计区分文件夹/视频并保持完整可见。
- 播放器输入门禁覆盖文本输入、菜单、弹窗和原生文件对话框；队列搜索关闭后恢复焦点。全屏队列离开热区后按 180ms 设置自动隐藏，Esc 优先退出全屏。
- Relink 取消选择不再留下旋转状态；备份页区分当前数据差异与 fingerprint 歧义；空标签显示中文字段错误；卡片“更多”补齐“打开位置”。
- 完整 180 项测试、`dart format`、analyze 和 Windows debug build 通过；1268×714 隔离窗口完成目录统计、筛选、菜单、空标签、逐键 `chamosan`、焦点恢复、全屏队列隐藏和 Esc 退出截图验证。

`0.2.16` 已完成：

- 左右侧栏结构动画增强为 320ms，并统一加入更明显的内容位移、淡入、轻微缩放和移动边缘阴影/描边过渡。
- 网格稳定列数、筛选状态、滚动位置、缩略图任务与播放队列均保持原逻辑；没有新增逐帧业务重算或动画 Controller。
- 166 项完整测试、`dart format`、analyze 和 Windows debug build 通过；最新 Debug 窗口完成左右栏往返开合及快速反向截图，三列稳定且无遮挡、裁切或溢出。

`0.2.15` 已完成：

- 设置首页按播放设置、数据与维护分组，只展示四个功能列表入口，实际控件全部进入对应二级页。
- 二级页保留原设置状态和持久化回调，提供独立标题、返回按钮与系统返回拦截；缓存统计刷新不再常驻设置首页。
- 165 项完整测试、`dart format`、analyze 和 Windows debug build 通过；最新 Debug 窗口已依次点击四个二级页并截图，长内容滚动、缓存加载态、对齐、溢出和返回状态均通过复测。

`0.2.14` 已完成：

- 结果网格在侧栏动画期间锁定列数而不是锁定旧视口宽度，卡片随可用空间连续缩放；跨越断点时只在动画稳定后单次换列。
- 左侧栏展开内容和折叠图标轨道各自保留目标几何，以淡入淡出和轻微位移替代中途阈值突变；右侧面板继续沿用 260ms 结构动画。
- 156 项完整测试、analyze、Windows debug build 通过，新 Debug EXE 响应正常；真实点击截图因当前 `computer-use` 入口不可用保留人工路径。

`0.2.13` 已完成：

- 顶栏与首行媒体卡片增加明确留白，卡片标题固定两行高度，解决短标题与长标题的视觉高度不一致。
- 左右侧栏统一为 260ms 结构动画；右侧面板增加淡入淡出和横向位移，旧面板只在切换期间保留。
- 结果区在侧栏连续改变宽度时保持稳定卡片尺寸，动画结束后只换列一次，避免缩略图逐帧拉伸和画面抖动。
- 156 项完整测试、analyze、Windows build 通过；Debug EXE 响应正常，真实点击截图因当前 `computer-use` 执行入口不可用留待精确人工复测。

`0.2.12` 已完成：

- 网格/列表切换改为单体滑块，整个控件共享点击、焦点和语义命中；每次点击翻转布局，选中底块用 180ms 动画左右滑动。
- 外框尺寸与顶部工具栏密度保持不变，图标不再分别绘制按钮底色或创建独立点击区域。95 项 widget、analyze、Windows build 通过。

`0.2.11` 已完成：

- 媒体库结果摘要压缩为 64px 单行工具栏，普通状态按可用宽度显示两到三个筛选标签及 `+N` 折叠数量，结果数量和多选入口保持在同一行。
- 多选状态整行切换为批量操作，卡片复选框替换收藏叠层且不启动播放或动态预览；宽度不足时结果文案允许收缩，避免挤压筛选标签。
- 95 项 widget、analyze、Windows build 通过；Debug EXE 响应正常，当前 Windows 自动点击入口不可用，真实多选与删除确认截图留待精确路径复测。

`0.2.10` 已完成：

- 主功能栏隐藏自动滚动条视觉并保留全部滚动输入；展开态、折叠态和本地 root 子列表使用统一侧栏滚动行为。
- 删除双左/双右箭头，紫色品牌图标成为唯一折叠按钮；展开态三角向右，折叠态三角向下，tooltip 与按钮语义同步切换。
- 93 项 widget、analyze、Windows build 通过；新 Debug EXE 响应正常，当前 Windows 自动点击入口不可用，真实折叠/展开截图留待精确路径复测。

`0.2.9` 已完成：

- 卡片标题右侧新增仅在 hover、键盘焦点或菜单展开时显示的更多按钮，120ms 淡入淡出且隐藏时不参与鼠标、语义或键盘命中。
- 固定 28px 按钮槽位避免标题换行抖动；不支持编辑/删除文件的复用卡片不预留槽位。菜单继续走统一标签编辑和删除确认流程。
- 93 项 widget、analyze、Windows build 通过；Debug EXE 响应正常，当前 Windows 自动点击入口不可用，真实悬停/菜单截图留待精确路径复测。

`0.2.8` 已完成：

- 动态预览首帧显示后隐藏右下角时长，移开后用同一 180ms 过渡恢复；加载阶段不提前隐藏。
- 收藏红心移除常态与交互态背景，保留响应式尺寸、点击范围、状态颜色和阴影。
- 92 项 widget、analyze、Windows build 通过；当前 Windows 自动化入口不可用，真实 hover 截图留待精确路径复测。

`0.2.7` 已完成：

- 视频卡片 hover 改为固定框内 `1.06x` 画面缩放，动画正反向从当前进度衔接，不再使用整块上浮阴影。
- 标签筛选折叠条压缩到 64px 总横向占用，网格默认留白和列间距收紧；1600/2000px 结果区保持四列，2200px 进入五列。
- 92 项 widget、analyze、Windows build 通过；当前 Windows 自动化入口不可用，真实快速横扫和折叠条截图留待精确路径复测。

`0.2.6` 已完成：

- 卡片动态预览使用可取消的 650ms 停留意图，快速掠过不会创建播放器或显示加载圈。
- 离开时用 180ms 淡回静态缩略图后释放预览；淡出期间重新移入复用现有播放器并立即恢复画面。
- 92 项 widget、analyze、Windows build 通过；1268px Windows 真实窗口覆盖快速横扫、稳定停留、退出和快速重入。

`0.2.5` 已完成：

- 缩略图加载、失败和空状态使用同一深色渐变占位体系，悬停动态预览加载层同步去除浅色闪屏。
- 卡片悬停取消整卡紫色描边与背景，仅缩略图上浮 3px 并显示轻量阴影，标题和下方信息保持原位。
- 90 项 widget、analyze、Windows build 通过；1268px Windows 真实窗口确认局部浮动、动态预览和固定标题位置。

`0.2.4` 已完成：

- 收藏按钮、红心、时长字号、边缘距离和角标内边距按卡片宽度使用三档响应式指标。
- 收藏与时长底色分别降为 46% 和 56% 黑色，通过轻量阴影兼顾更低遮挡和亮色画面可读性。
- 1268px Windows 真实窗口已覆盖亮暗缩略图与收藏切换反馈；用户窗口空闲后恢复原状态，收藏总数回到 5。

`0.2.3` 已完成：

- 媒体库标题字号、缩略图圆角、列间距和行间距按结果区宽度分档，卡片高度与网格列数继续共用同一响应式计算。
- 超宽结果区提高单卡最大宽度，2200px 结果区从 6 列收敛为 5 列，普通 1268px 窗口保持三列布局。
- 1268px Windows 真实窗口完成截图验收；宽屏二次复查因检测到用户正在操作其它窗口而停止，纯函数测试覆盖宽屏边界。

`0.2.2` 已完成：

- 媒体库主功能栏可在完整导航与 76px 图标轨道之间切换，动画期间按真实宽度切换内容，避免窄宽溢出。
- 媒体库使用局部深色主题 token 统一侧栏、工作区、搜索、筛选、标签面板与卡片层级，不改变其它页面的全局主题。
- 网格卡片改为轻量信息流结构，去除常驻浅色外壳和重阴影，折叠侧栏后列数可随可用宽度自然增加。
- 真实 1268×714 Windows 窗口完成折叠/展开、标签面板、播放器进入/返回截图验收，无明显抖动、遮挡或溢出。

`0.2.0` 已完成：

- 通过 app theme 统一弹窗基线：浅色 surface、边框、8px 圆角和更强标题层级。
- 统一媒体库侧栏间距和动作按钮行为；窄 side sheet 下目录动作会换行而不是 overflow。
- 统一视频卡片行为：响应式网格间距、compact 单列尺寸、稳定底部操作行和固定图标按钮尺寸。
- 统一缓存诊断与媒体库页面间距；compact AppBar 动作折叠进菜单。
- 统一 Tag Manager 基础布局，并实现 `expanded` / `medium` / `compact` 响应式行为：
  - `expanded`：常驻 360px 标签管理侧栏。
  - `medium`：常驻但更窄的 316px 管理侧栏。
  - `compact`：纵向列表 / 详情布局，无横向 overflow。
- 播放器深色队列侧栏仅在宽度允许时常驻；compact 时从 AppBar 打开 bottom sheet。
- 媒体库 compact 筛选 BottomSheet 使用与其它页面一致的浅色 surface 和 8px 顶部圆角。

未改变：

- SQLite schema。
- `TagQueryService`、`FilterQuery` 或 `TagQueryContext`。
- 播放器筛选队列语义、播放器 open worker 或播放 backend。
- 缩略图队列、FFmpeg/FFprobe backend 行为或缓存诊断逻辑。
- Smart List、relink/missing、文件移动、标签删除或标签合并 migration。

## macOS / Linux 适配说明

- FFmpeg bundled tools：当前 Windows 布局期望 `.exe` 工具位于 `tools/ffmpeg/bin`；macOS / Linux 打包需要平台专属二进制、可执行权限、macOS quarantine / signing 检查，以及按平台分离查找顺序。
- sqlite3 动态库：Windows 当前打包 `sqlite3.dll`；macOS / Linux 需要 `.dylib` / `.so` 放在 Flutter desktop 打包和运行时库搜索路径兼容的位置。
- 文件管理器定位：Windows reveal 行为在 macOS 应映射到 Finder `open -R`，在 Linux 映射到具体桌面环境的 reveal/open 命令；无法精确定位时回退打开父目录。
- 窗口尺寸：当前桌面 UX 在 `expanded` 最佳；macOS / Linux 应设置合理最小窗口尺寸，并测试 compact / medium，因为平铺窗口管理器可能产生很窄的桌面窗口。
- 快捷键：验证 macOS 上 Command / Control 约定、Delete / Backspace 差异、功能键行为，以及 Linux 桌面环境中的鼠标侧键可用性。

## 新对话提示

```text
这是 Chat 7 / 响应式 UI + 平台 polish。项目路径：<project-root>。
请先阅读：
- PROJECT.md
- ARCHITECTURE.md
- CURRENT_TASK.md
- ROADMAP.md
- <private-planning-document>
- docs/chat_tasks/CHAT_7_RESPONSIVE_UI.md

职责：视觉一致性、响应式布局、桌面平台 polish 和 macOS/Linux 适配说明。不要修改标签查询语义、播放器 backend、FFmpeg 内部逻辑或 SQLite schema。
修改代码后运行：
- flutter analyze
- flutter build windows --debug
```

## 变更记录

- `0.3.0`：完成 Apple UI Phase 0，共享 token、无障碍策略与基础交互表面通过测试和真实窗口验证。
- `0.3.0-plan`：建立 Apple 式全应用 UI skill 与六阶段迁移蓝图。
- `0.2.16`：增强左右侧栏的位移、缩放、淡入和移动边缘层次，保持网格列数与业务状态稳定。
- `0.2.15`：设置首页按功能类型分组，实际控件进入四个可返回的二级页。
- `0.2.14`：修正侧栏动画期间旧宽度网格裁切和左栏中途内容突变，改为锁列数连续缩放与稳定后单次换列。
- `0.2.13`：增加媒体卡片固定标题高度、顶栏内容留白、左右侧栏结构动画和结果网格单次重排缓冲。
- `0.2.1`：修复设置页硬件解码下拉取消确认后仍残留新选项显示的问题；解码切换控件抽为独立可测 widget，保持确认后才保存设置。
- `0.2.0`：完成第一阶段响应式 UI polish，并增加 macOS / Linux 适配说明。
- `0.1.0`：从 `local_tag_player_flutter_cross_platform_plan_v2.md` 创建任务。
