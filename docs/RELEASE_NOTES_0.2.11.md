本次更新

- 修复连续输入和扫描提交交错时搜索停留在旧关键词的问题，最新输入会继续执行；没有内容变化的成功扫描复用搜索索引。
- 改善大库搜索对短 Unicode 词及特殊字符标签别名的匹配，减少重复索引工作，并降低扫描进度和标签面板动画的重复构建。
- 暂时不可读或缺失的视频保留记录、标签、收藏和观看进度；媒体库加载失败支持重试，首次使用和空筛选结果提供明确操作入口。
- 更新文件选择相关依赖，目录、文件和保存入口保持平台适配；保存取消时不创建空文件。

数据安全

- 保留现有标签来源区分、筛选语义和来源播放队列；本次发布准备没有修改数据库结构或业务代码。
- 无效记录清理通过设置页明确确认，不删除磁盘媒体文件。升级或回退前建议备份应用数据。

安装包说明

- 提供 Windows x64 安装器和 macOS DMG。本次 Windows 安装包未签名，macOS 安装包未签名且未公证，系统可能显示安全提示。
- 发布页附带 SHA-256 校验文件，可用于核对下载完整性。
- 大库首次索引与慢帧优化仍在继续；原生安装升级及部分交互验收尚未完成，不代表所有性能目标已经达标。
